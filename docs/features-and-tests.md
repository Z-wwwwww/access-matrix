# 基盤機能 & テストポイント / 基盘功能 & 测试点

> **维护约定（重要）**：本文件是**当前功能 + 怎么用 + 手动测试点**的单一来源——描述"这个项目有哪些功能、怎么用"的**现状快照**,不是变更日志。
> **每次新增或改动功能,就地更新对应小节**使其反映最新行为,并保持其测试点最新(可勾选清单)。**只同步最新状态,不记录历史/变更履历。**
> AI 助手在实现或修改功能后也应主动更新本文件,把它当作"完成"的一部分(像更新测试一样)。

约定标记:`mode=oidc` 指 Keycloak SSO 模式(dev/prod 默认);本地 dev 账号见 `docs/getting-started`。
平台运维(ops)= `system` 租户用户;业务用户 = 各业务租户(demo / acme …)。

---

## 0. 認証 / ログイン方式（Auth / Login）

| 方式 | 说明 |
|---|---|
| **OIDC SSO** | 主路径。各租户对应一个 Keycloak realm;平台运维用 `system` realm。登录后 JWT 带 `tid`(租户)。 |
| **Break-glass 密码登录** | SSO 不可用时的应急通道(`AdminAuthController` HS256)。`mode=oidc` 下 `/auth/login` 成功即视为 break-glass,会写审计 `system/auth.breakGlass` + 给本人发告警邮件。仅 super-admin 的 `password_hash` 被保留。 |
| **安全模式** | `app.security.mode` = `oidc`(默认) / `jwt`(legacy 密码) / `permit-all`(裸跑无 IdP)。 |
| **租户解析** | `utils/tenant.js`:子域名 → `?tenant=` → localStorage → `default` 的优先级。 |
| **会话维持** | 双模式。SSO 模式:KC access token(30 min)+ KC refresh_token(SPA 持有,`kc_refresh_token`),到期前 ~2 min 主动续期(`stores/auth.js` 定时器,直打 KC token endpoint,同时重置 KC SSO 空闲计时——开着的标签页不再 30 分钟被踢回 KC 登录页,直到 `ssoSessionMaxLifespan` 才需重登);密码/break-glass 模式:refresh token HttpOnly cookie 走后端 `/auth/refresh`。两条路都受 401 单飞刷新 + 原请求重放兜底;刷新失败(KC 会话真正结束 / 被踢)踢回登录。支持会话(impersonation)期间不续期(避免覆盖 support token)。依赖 realm `revokeRefreshToken=false`(多标签页并发续期不互踢)。 |
| **入驻** | 业务租户管理员:邀请邮件 + 落地页设密;平台运维用户:一次性临时密码(KC 强制首登改密)。 |

**测试点**
- [ ] SSO 正常登录(各租户 + system),登录后能进对应首页。
- [ ] SSO 不可达时,登录页出现"使用应急密码登录"通道;break-glass 登录成功。
- [ ] **破玻璃之后不会被拖去 KC(bug 修复)**:让 KC **可达但慢**(例如给 `/.well-known/openid-configuration` 加 1~2s 延迟),打开 `/login`,在探测还没返回时就连点 5 次热区(或点横幅的"使用应急密码登录")→ 页面**停在密码表单**,不会在随后 1.5 秒被跳去 Keycloak。同理,探测在飞行中时离开 `/login`(如打开 `/invite/{token}`)也不该被拽回 KC。机制:自动跳转是在**探测 resolve 之后**才 arm 的(探测最长 3s,`SSO_PROBE_TIMEOUT_MS`),而那之前根本没有 timer 可清 —— 所以取消动作除了清 timer 还要 latch 一个 `autoRedirectCancelled` 标志,`.then` 里再复查它与 `shouldAutoRedirectToSso()`;`onBeforeUnmount` 也走同一条取消路径。此前只判 `if (autoRedirectTimer)`,于是"慢 KC + 抢在探测期破玻璃"正好落进空窗 —— 而慢 KC 恰恰是最需要破玻璃的场景。
- [ ] break-glass 登录后:`core_oplog` 有 `system/auth.breakGlass` 记录;安全面板"break-glass(7天)"+1;本人收到告警邮件(或日志)。
- [ ] **break-glass 密钥强制(所有模式)**:`CORE_JWT_SECRET` 必须 ≥32 字节且不含任何已知模板标记——`JwtSecretValidator` 对 `dev-placeholder`/`dev-local`/`throwaway`/`change-in-prod`/`do-not-use` 做大小写不敏感的**子串**匹配(不只前缀),故照抄 `.env.example` 的占位密钥(任何环境,含 dev)启动即 fail-fast;oidc 模式下应急通道(HS256)解码器同样挂载,缺失/占位同样拒绝(不再只有 mode=jwt 才校验)。设置合法强随机值(`openssl rand -base64 48`)后,break-glass `/auth/login` 各租户均可用(业务租户登录不再误报 `tenantSuspended`)。
- [ ] **租户隔离的 Java 侧兜底也必须 fail-closed(bug 修复)**:把 `application.yml` 里整个 `app.mybatis` 段注释掉后启动 → 租户隔离仍然**生效**(跨租户查不到别家数据),不是静默放开。此前 `AppMybatisProperties` 的兜底是 `new Tenant(false)`:配置子树一缺失,MyBatis-Plus 租户拦截器就根本不装,所有查询都不带 `WHERE tenant_id = ?`,**每个租户都能读到其它租户的行**,而且没有任何信号 —— `TenantSchemaGuard` 恰好也被同一个开关关掉(只留一条 INFO)。而 yml 注释、`.env.example`、以及同包 `AppSecurityProperties` 里每一个安全兜底(rateLimit/lockout/refreshCookie.secure 全默认安全值)都在声明相反的约定。真正无租户上下文的 profile 现在必须**显式**写 `false`,这正是 yml 注释原本就规定的。由 `AppPropertiesDefaultsTest` 钉住(同时把 security / cors 的兜底方向一起钉上)。
- [ ] **业务租户 break-glass 不误报停用(bug 修复)**:对业务租户(非 system)用应急密码 `/auth/login` 成功,不再因 `core_tenant` 注册表被租户拦截器误加 `tenant_id` 过滤而返回 `error.auth.tenantSuspended`(`TenantMapper.findActiveByCode` 标 `@InterceptorIgnore`)。
- [ ] **失败登录的审计行不会因为输入超长而消失(bug 修复)**:用超长输入打 `POST /api/auth/login` —— 例如 `username` 200 字符、`User-Agent` 4000 字符、`X-Tenant-Id` 200 字符 → `core_auth_login_log` **仍有那一行**(各字段截到列宽:identifier 128 / user_agent 512 / tenant_id 64 / client_ip 64 / failure_reason 128)。此前 `LoginAuditService.record` 与 oplog 是同一个姿势:insert 包在 catch-and-WARN 里(审计绝不能影响登录),于是 Postgres 的「值太长了」**不报错、只是让这条失败登录记录消失**。而这里每个入参都是**未认证**请求可控的 —— `identifier` 就是 `LoginRequest.username`(只有 `@NotBlank`,没有 `@Size`)、`userAgent` 是原始请求头、`tenantId` 是原始 `X-Tenant-Id`;已在真实库上验证 identifier/129、user_agent/513、tenant_id/65 都被直接拒。后果是:Redis 侧的锁定计数照常涨,审计里却一片空白 —— 相当于一个自助的「无痕暴力破解」姿势。修法与 oplog 一致,在唯一写入点按列宽夹取;截断的审计行远好过没有。由 `LoginAuditServiceTest` 钉住。
- [ ] access token 过期后自动刷新、原请求重放成功;刷新失败被踢回 `/login`。
- [ ] **SSO 长会话不被踢(bug 修复)**:SSO 登录后保持页面打开/持续操作超过 30 分钟(access token 寿命),**不**跳回 KC 登录页——前台在到期前 ~2 min 自动用 KC refresh_token 续期(Network 可见对 `/protocol/openid-connect/token` 的 `grant_type=refresh_token` 请求);KC 管理台该用户 Sessions 的 idle 时间被刷新。管理员踢下线/禁用后,续期立即失败(invalid_grant)→ 正常踢回登录,强制登出不受影响。
- [ ] 会话失效(被踢下线 / 停用 / 过期且刷新失败)→ 下次请求(如 `/menu/me`)跳 `/login` 并显示友好「会话已过期或已被登出,请重新登录」,**不**再显示技术性的「Menu load failed — 401」(router 守卫把 401 与真正的 menu 加载错误 5xx/坏数据 分开处理,后者才带 detail 供排障)。
- [ ] **被 JIT 解析器拒绝的请求真的"没有业务用户"(bug 修复)**:让一个被禁用/已删除/已迁出 SSO 的账号(三条拒绝分支任一)带着仍然有效的 token 调 `PUT /api/me/profile`(这是唯一一个**不需要权限**却带 `@OpLog` 的端点)→ `core_oplog` 应当留下一行 `system / profile.update`、其 `user_id` 为 **NULL**;此前是**一行都没有**。原因:`OidcJitUserService` 三条拒绝分支的注释都写着「resolves to no business user → no access」,但 `CoreRequestContextFilter` 只在解析**成功**时才覆盖 userId —— 拒绝时 `RequestContext.userId` 仍是 36 字符的 **Keycloak UUID**(既不是业务身份、也不是"没有身份")。而 `core_oplog.user_id` 是 `character(26)`,已在真实库上验证 36 字符的 KC UUID 被直接拒(「对于字符类型来说这个值太长了(26)」),`OpLogService` 又把 insert 失败吞掉 —— 于是这次请求的审计行整条消失。修法在**产生错误值的地方**:拒绝即把 userId 置空(该列可空,已查证),而不是在下游截出一个无意义的 26 字符前缀;顺带消掉「某个免权限端点将来按 userId 取数」这个长期隐患。业务影响仅限审计:该 userId 匹配不到任何行,所以写操作本来就是空操作,`/menu/me` 返回空菜单 → 前端照常登出。由 `CoreRequestContextFilterTraceIdTest` 钉住 2 条(拒绝→null、成功→业务 ULID)。
- [ ] **已登录用户被删除后刷新页面 → 直接登出到 `/login`(bug 修复)**,而非 404。删除后其 token 解析不出可用菜单(角色已解绑;且旧业务行 `mark=0`),`/menu/me` 返回空菜单 → router 守卫检测到「无 home / 无可用路由」即 `clearAuth` + 跳 `/login`,不再注册空 layout 让刷新路径落到 404 兜底页。
- [ ] **邮件落地页在未登录状态下可直接打开(bug 修复)**:退出登录(或用无痕窗口)后,直接访问 `/invite/{token}` 与 `/reset-password/{token}` 都应渲染各自的设密页面,**不**被 router 守卫弹回 `/login`。`/reset-password/` 尤其关键——它是 SSO → 密码反向迁移邮件(`SsoToPasswordMigrationService`,链接为 `CORE_APP_BASE_URL + /reset-password/{token}`)的落地页,收件人按定义还没有密码、根本无法登录,漏掉白名单会让整条重置链路走不通。守卫的公开路径集合由 `router/index.js` 的 `isPublicPath()` 单点判定,并由 `tests/components/routerPublicPaths.test.js` 钉住。
- [ ] **非 demo 租户的邀请/重置链接不再一律「无效或已过期」(bug 修复)**:给一个**非 demo** 租户(如 `sozonext`)的用户发邀请,用**邮件里的链接原样**打开(即从全局 `CORE_APP_BASE_URL` 的域名进,不带 `?tenant=`、localStorage 也是空的,例如无痕窗口)→ 应正常进入设密页并能设成密码;`/reset-password/{token}` 同理。此前这四条语句(两张表各 `findActiveByTokenHash` + `markUsed`)**都没有 `@InterceptorIgnore`**,而它们的 javadoc 却写着「hand-written @Select —— pre-auth,所以 `RequestContext` 里没有租户」—— 手写 SQL **并不能**豁免租户拦截器。代码库自己在 `TenantMapper.findActiveByCode` 的注释里就写明了这条机制:「MyBatis-Plus 租户拦截器改写**所有** SQL —— 包括手写 `@Select` —— 没有 `@InterceptorIgnore` 就会注入 `AND tenant_id = ?`」。而 pre-auth 的上下文租户来自 `X-Tenant-Id`,前端是按**子域名**推的;邮件链接却是用全局唯一的 `app.mail.base-url` 拼的,所以首次收信人(localStorage 为空、域名是 apex/保留标签)会落到兜底值 `demo`。已在真实库上验证:`sozonext` 的 token 用本意 SQL 查得到,加上 `AND tenant_id = 'demo'` 后**查到 0 行**;`markUsed` 的 UPDATE 同样**影响 0 行**,而调用方按约定要把 0 行读成「已使用」。净效果是:**demo 之外每一个租户的邀请链接和密码重置链接都报「无效或已过期」**——而 dev/QA 都跑在 `demo` 上,所以一直看不见。现在四条语句各自加上 `@InterceptorIgnore(tenantLine = "true")`(逐方法,不是类级别;`token_hash` 有 `mark=1` 的全局唯一索引,消费方的租户/用户一律取自**刚认领到的那行**、绝不取自请求)。由 `PreAuthTokenMapperScopingTest` 钉住,并额外断言这个豁免**只**给这两条语句、不许扩到类级别。
- [ ] **邮件链接严格一次性(bug 修复)**:`/invite/{token}` 与 `/reset-password/{token}` 的 accept 都必须只成功一次。手工验证:提交一次成功后回退浏览器再提交 → 报「已使用/无效」;并发双击(或用 curl 并行发两次同一 token 的 POST)→ **只有一个成功**,另一个报错。两条链路的 `consume()` 都以 `markUsed` 的**影响行数**作为认领凭据(`UserInviteMapper.markUsed` / `PasswordResetTokenMapper.markUsed`),行数为 0 即拒绝;`InviteTokenServiceTest` / `PasswordResetTokenServiceTest` 各自钉住这一条。
- [ ] **业务租户 realm 必须开着 KC 暴力破解防护(安全缺口修复)**:在 KC 管理台看任一业务租户 realm 的 Realm settings → Security defenses → Brute force detection,应为 **启用**;新建一个租户后同样应为启用(模板 `infra/keycloak/realms/demo-realm.json` 的 `bruteForceProtected`,`KeycloakRealmService.createRealmFromTemplate` 克隆它)。为什么必须靠它:oidc 模式下业务用户走的是 **KC 自己的登录表单**,应用侧两道防线都覆盖不到——`AccountLockoutService` 只在 `AuthService.login`(即 break-glass 的 `POST /auth/login`)触发,`AuthRateLimitFilter` 只过滤本服务 URI 里含 `/auth/` 的请求,都碰不到 KC 的 token 端点。此前模板里该开关是 `false`(而 ops 的 `system-realm.json` 是 `true`),等于每个业务租户的密码都可被无限次在线猜测。由 `KeycloakRealmTemplateTest` 钉住渲染后的克隆 JSON。**注意:已按旧模板建出来的 realm 不会被追溯修复**,`KeycloakRealmService` 没有「更新 realm 安全设置」的接口,存量租户需在 KC 管理台逐个打开(或跑一次性脚本)。
- [ ] 多 realm:在 A 租户的账号无法登录 B 租户。
- [ ] 新建运维用户用临时密码首登 → KC「修改密码」页,按钮是通用「提交」(不是「发送重置链接」)。Keycloak 主题 `doSubmit` 必须保持通用(多页面共用),不要写成某页专属文案。
- [ ] KC 多字段表单(更新账户信息 / 修改密码)排版整齐:必填 `*` 紧跟 label **同一行**,label↔input、字段↔字段间距正常(不因 `.pf-*__label-text` 被设成 block 而拉开)。
- [ ] **右上角「修改密码」直达 KC 改密表单(UX 修复)**:用户菜单 → 修改密码 → 点击按钮后整页跳转,**直接落在** Keycloak 的「修改密码」页(`kc_action=UPDATE_PASSWORD` 应用发起动作,走带 PKCE 的标准授权流,`oidc.js beginPasswordUpdate()`),保存(或取消)后经 `/sso/callback` 自动回到出发页面、应用会话同时刷新。不再新开账号控制台首页(改密入口藏在「帐户安全 → 登录」两层深,用户找不到)。
- [ ] **SSO 页跟随应用明暗**:在应用内切到深色(即使系统为亮色)后点登录跳到 KC,登录页为深色;切回亮色则 KC 为亮色;登录失败重渲染后仍保持(sessionStorage `am_ui_mode` 兜底)。亮色保持 navy/gold 品牌入口;**暗黑模式套用应用暖黑配色**——暖近黑背景 + 暖米白文字 + 陶土橙强调(主按钮/输入聚焦环/复选框选中/链接 hover),金色 logo 保留。机制:前端在 `/authorize`(及忘记密码 `reset-credentials`)URL 追加 `ui_mode=dark|light`(`oidc.js`,取自 `<html>.dark`),KC 主题 `login.js` 读取并在 `<html>` 设 `data-theme`(头部同步脚本,无闪烁);KC `access-matrix.css` 的 `[data-theme="dark"]` 重定义 `--am-*` token。改 KC 主题文件需**重启/重同步 KC**(`start-keycloak.bat` 每次启动 xcopy 同步)才生效。

---

## 1. マルチテナント & RBAC 基盤

- **多租户**:业务行带 `tenant_id`;`system` 租户(平台运维)被 MyBatis-Plus 拦截器识别为"绕过租户范围",可跨租户读写。隔离开关 `app.mybatis.tenant.enabled` **两端都 fail-closed**:yml 默认 `true`,且 `AppMybatisProperties` 在整个 `app.mybatis` 子树缺失时也兜底为 `true`(见下测试点)。
- **权限通配符**(`PermissionMatcher`,前后端同源 `frontend/src/utils/permission.js`):`*:*`=平台超级(仅匹配 `platform:` 命名空间);`tenant:*`=租户超级(匹配业务权限,即 `platform:` **与** `opsuser:` 之外);`resource:*`=某资源全动作;精确匹配。两个超级互不覆盖;`opsuser:`(平台用户管理)是独立保留命名空间,`platform:*`/`*:*`/`tenant:*` 都不自动覆盖,只有显式 `opsuser:*` 才命中。
- **角色**:SUPER_ADMIN(各租户,`tenant:*`)、PLATFORM_ADMIN(ops,`*:*`+`opsuser:*`)、PLATFORM_OPERATOR(常规运维,`*:*`)。
- **超管唯一且锁定**:每个业务租户**有且仅有一个** SUPER_ADMIN——即租户新建时邀请的那个用户。该内置「Super Administrator」角色**不可转移**:`UserAdminService.assignRoles` 拒绝把它授予第二个用户(`error.user.superAdminSingleton`);唯一持有者整账号受保护(见下条),其角色绑定任何人都动不了,自然也剥离不掉;内置角色本身已被 `assertNotBuiltIn` 全锁(改名/删除/改权限均拒)。**「同级别」也封堵**:自定义角色不能绑定 `tenant:*`/`*:*` 通配权限(`RoleAdminService.bindPermissions` → `error.role.superPermissionReserved`),避免造出等效超管。前端 `UserEdit` 角色选择器对非持有者隐藏超管角色、对持有者只读;`RoleEdit` 权限选择器对自定义角色隐藏 `tenant:*`/`*:*`。
- **租户管理员账号(=超管用户)在用户管理界面完全只读**:对**其他任何用户**(无论持有什么用户管理权限)整账号不可操作——编辑(含邮箱/显示名)/改角色/改部门/改状态/删除/重置密码/强制登出全部拒绝;管理员**只能本人**在「个人资料」页改自己的邮箱/显示名(`updateOwnProfile`)。识别走后端 `UserDto.View.superAdmin`(以及内置 admin 的 `builtin`)标志位下发,前端 `User.vue` 对该行**直接不渲染任何操作按钮**(操作列为空)。后端硬守卫:`UserAdminService` 各变更方法统一走 `assertNotProtectedAdmin`(`error.user.adminProtected`);该守卫同时**取代了旧的「最后一个超管」守卫**——唯一超管整账号动不了,租户恒有一个可用超管。(回归:此前 `update` 仅锁部门/状态、邮箱/显示名对任何 user:update 持有者开放,且 `changeDept` 完全没拦超管。)
- **不能在用户管理里操作自己 + 自助改资料走个人页**:用户管理控制台对**调用者自己那行**拒绝一切变更(编辑/删除/停用/强制登出/改角色/改部门)——后端 `UserAdminService` 各变更方法首行 `assertNotSelf`(`error.user.selfManagementForbidden`),前端 `User.vue` 按 `isSelf(row)=row.id===authStore.userId` 禁用该行所有按钮 + 提示「请在个人资料页管理自己」。这同时堵两个坑:**自我提权**(给自己加角色)和**自我锁死**(停用/删除/踢自己)。自助改自己**邮箱/显示名**改走「个人资料」页(`Profile.vue` 现可编辑 → `PUT /user/me/profile` → `UserAdminService.updateOwnProfile`,仅联系字段,登录即可、无需细权限);部门/状态/角色仍只能由管理员改。
- **改邮箱/显示名同步 Keycloak**:租户用户管理「编辑」(`UserAdminService.update`)和个人资料页自助修改(`updateOwnProfile`)在写本地 DB 前先 `kc.updateProfile`(email 保持已验证、姓名按显示名重切),与平台运维用户管理同一行为;KC 失败则报错且本地不写,两边不漂移。此前租户侧只写本地,KC 一直保留旧值——账号控制台显示旧邮箱,且 KC「忘记密码」邮件会发往**旧地址**。KC 未启用或该行无 `keycloak_id` 时跳过。
- **编辑路径也做邮箱重复预检**:邮箱可用于登录(`findByIdentifier` 匹配 `email = ? … LIMIT 1`),重复会导致邮箱登录命中歧义。三条编辑路径(租户管理员编辑 / 个人资料自助修改 / 平台运维用户编辑)现与 create 一样做租户内精确预检(排除被编辑行自身),命中抛 `error.user.emailExists` / `error.opsuser.emailExists`(按语言显示);预检在 KC 同步**之前**,KC 不被触碰。此前编辑路径无预检——OIDC 模式仅靠 KC `duplicateEmailsAllowed=false` 兜底(报错笼统误导),legacy password 模式则能直接把重复邮箱写进库。
- **管理员重置业务用户密码(与平台侧同一流程)**:用户管理「重置密码」**不再让管理员输入新密码**——`POST /admin/user/{id}/reset-password`(权限 `auth:reset-password`)由后端生成一次性临时密码:OIDC 模式写入 **Keycloak**(`temporary=true`,首登强制自设新密码;本地 `password_hash` 不动,维持 as-if-always-OIDC 不变式);legacy password 模式才落本地 hash。同时 `terminateUser` 踢下线(拒旧 token + 结束 KC 会话)+ 尽力发「密码重置」邮件(`user-direct-welcome` 模板 reset 文案,走应用 `CORE_MAIL_*`),临时密码**只回显一次**(前端一次性抽屉 + 复制按钮,与平台运维用户管理同款 UX)。对自己、内置 admin、租户超管均拒绝(`selfManagementForbidden` / `adminProtected`)。旧的「管理员手输新密码」端点 `/admin/auth/reset-password` 已删除;**自助改密入口只保留在右上角用户菜单**(KC 账号控制台;超管另有 break-glass)。
- **新建租户自动获得细粒度权限目录**:`RbacSeederService.seedDefaultsForTenant` 除了种 `tenant:*` + 超管角色,还按 `PermissionRegistry` 把**业务侧**(非 `platform:`/`opsuser:`,即 module≠`platform`)的细粒度权限全部种进该租户的 `core_rbac_permission`,这样租户管理员能给自定义角色配权限。**权限目录仍按租户存**(`demo`/业务租户=业务权限,`system`=平台权限),天然隔离平台/业务。此前新租户只有 `tenant:*`——又对自定义角色隐藏——导致角色权限选择器全空、自定义角色拿不到任何权限。**新增功能的权限自动同步到所有租户**:`TenantPermissionCatalogReconciler`(启动期 `ApplicationReadyEvent`)遍历所有业务租户、对每个调用幂等的 `seedPermissionCatalog`,所以加完新页面/功能(新增 `@RequiresPermission` 常量)**重启后端**后,新权限码会自动出现在**每个已有租户**的角色权限选择器里(不再只对新建租户生效)。`system` 租户跳过(平台运维目录);新增只插不删,与 `PermissionConsistencyGuard` 的跨租户孤儿清理(只删代码里已移除的)互补。
- **无可用菜单时不再无限跳转**:零权限/未分配角色的用户登录后,菜单为空 → 守卫清登录态跳 `login?reason=no-access`(区别于 `session=expired`),登录页**不再自动重定向 SSO**(否则 KC 静默重登→菜单仍空→死循环),而是停在登录页提示 `login.message.noAccess`「账号暂无可访问页面,请联系管理员分配角色」。真正的会话过期(401)仍走 `session=expired` + 自动 SSO(有菜单则无缝重登)。
- **菜单**:全局一套(`core_rbac_menu`,无 tenant_id),按 `permission_code` 过滤;前端由 `menu-to-routes` 动态注册路由。
- **数据范围(data scope,部门级行可见性)**:范围定义在**角色**上(`core_rbac_role.data_scope`,字典五模式:ALL / 本部门+子 / 本部门 / 仅本人 / 自定义部门)。请求时 `DataScopeResolver` → `DataScopeQueryService.loadDecision` 算出 `{unrestricted, visibleDeptIds, selfOnly}`:多角色**取并集**(更宽松),任一 ALL 角色或 `*:*` 短路为全可见;部门子树按 `core_rbac_dept.path` 物化路径一条 LIKE 展开;CUSTOM 按角色绑定部门(各含其子树)、与用户自身部门无关;Caffeine 按 userId 缓存 5min,改角色/调部门经 `PermissionCacheService.evictUser` 即时失效。SQL 落地是服务层**显式**调 `DataScopeHelper.apply(wrapper, decision, deptCol, creatorCol)` → 独立 AND 组 `(dept_id IN (可见部门) OR create_user = 本人)`;零范围 → `1=0` 全拦(fail-closed)。**fail-closed 的两个推论**:① 无部门用户的 DEPT/DEPT_AND_SUB 角色贡献为零(只持这类角色 → 看不到任何行);② `dept_id=NULL` 或 `create_user='system'` 的行(程序自动生成且未归属)对所有受限用户不可见、仅 ALL/超管可见——自动写入的数据落库时应显式归属 `dept_id`/`create_user`。数据行的 `dept_id` 创建时盖章、**不跟人走**:用户调部门后旧部门行默认不可见(例外:SELF 看自己创建的、新部门是旧部门祖先、CUSTOM 含旧部门、ALL)。防遗忘:Mapper/服务方法标 `@DataScope`,`DataScopeAspect` 校验 wrapper 经过了 apply(dev/test 拒绝、prod WARN)。**CUSTOM 绑定的失效防护**:部门**删除**时统计 `role_dept` 引用并 IN_USE 提示(force 删除级联清绑定);部门**停用**(status 1→0)同样统计引用、有 CUSTOM 角色引用时 IN_USE 提示「N 个角色把该部门设为自定义数据范围」、确认后 force 重发才停用;解析时绑定指向**已删除**(mark=0)部门的条目贡献为零(fail-closed,不再裸加 id);RoleEdit「数据部门」tab 会把"已绑定但不在树里"(停用/删除)的部门列成**失效绑定警告区块**(琥珀色),可单个或全部移除。行级可见性与功能权限(`@RequiresPermission`)正交;租户隔离在更外层(MP 租户拦截器)。详例:`docs/data-scope-demo`。

**测试点**
- [ ] 业务 super(`tenant:*`)看不到平台菜单 / 调 `/platform/*` 返回 403。
- [ ] 平台 ops(`*:*`)能管租户但不能模拟业务用户(无 `tenant:*`)。
- [ ] **踢出/禁用/删除/重置密码会结束 KC 会话(bug 修复)**:业务用户被禁用/删除/踢下线/重置密码后,被踢者在登录跳转页**不会被 SSO 静默重新登录**——`UserAdminService` 经 `SessionTerminationService.terminateUser` 同时 `kickOut`(拒旧 token)+ `kc.logoutUser`(结束 KC 会话,逼真正重新认证)。此前只 kickOut、KC 会话还在,跳转即被静默重登,踢出形同无效。
- [ ] **被禁用的用户在 SSO 侧也一定进不来(纵深防御补漏)**:把用户 X 停用,然后**让 Keycloak 短暂不可达**(或在 KC 管理台手动把 X 重新 enable 模拟 KC 侧没生效),X 再走 SSO 登录 → 应当**登不进业务系统**(前端登出、后端日志一条 WARN 指明 `core_auth_user.status` 非启用、且提示 KC 的 `setEnabled(false)` 可能没落地);启用状态的用户不受影响;`status` 为 NULL 的历史行仍按启用处理。此前禁用是一次**两侧写**:DB 标记 + Keycloak `setEnabled(false)`,而**只有 KC 那一半能挡住新的 SSO 登录** —— 偏偏 `SessionTerminationService` 对 KC 这步是 **best-effort**(注释原文:「a KC hiccup must not block the local kick」),失败只记 WARN 就吞掉。于是 KC 在停用瞬间不可达时:`status=0` 照常提交、Redis kick 让**当前** token 失效,然后此人重新登录一次 —— KC 里他还是 enabled、新 token 的 iat 晚于 kick 所以 `ForceLogoutFilter` 放行、而 `OidcJitUserService` **一处都没有引用 `status`**,直接把业务 id 还给他。控制台显示已停用、人却照用 —— 正是项目里记过一次的 `sozo-admin2` 事故,而当时只在 Keycloak 一侧修了。现在 OIDC 解析路径也把 DB 标记当权威(与 `AuthService.login` 一直以来对 break-glass 路径的做法一致),快路径与 legacy 绑定路径都加了同一道闸;绑定路径**特意放在 bind UPDATE 之前** —— 否则一次被拒的登录会顺手清掉该用户的 `password_hash`,把他的应急凭据也一并毁掉。由 `OidcJitUserServiceTest` 钉住 4 条(禁用被拒 / 启用照常 / NULL 视为启用 / legacy 禁用被拒且不写 UPDATE)。
- [ ] **禁用用户后无法再登录(bug 修复)**:禁用时**同步禁用 Keycloak 用户**(`kc.setEnabled(false)`),启用时 `setEnabled(true)` + 清除 kick。光把 DB `status=0` **拦不住 SSO**——KC 仍会认证该用户、OIDC JIT 解析器也不查 status 就放行(这正是 `sozo-admin2` 禁用后仍能登录的原因)。**业务用户(`UserAdminService.changeStatus`)与平台运维用户(`PlatformUserAdminService.setEnabled`)的"有效/无效=停用/启用"副作用现在共用同一处** `SessionTerminationService.applyEnabled(userId, enabled)`(开关 KC + kick/clear + 停用时结束 KC 会话);两个 console 各自只管 DB status 写 + 自己的守卫,杜绝再次漂移。**UI 也对齐**:业务「用户管理」的状态徽标(`success`/`danger` 绿/红 + `common.status.enabled/disabled` 文案)和开关动作(`Pause`=停用 / `Play`=启用 两态按钮)与平台运维用户管理一致(原先是单个 `Power` 切换 + dict 徽标)。
- [ ] **删除用户不会再生成"幽灵"账号(bug 修复)**:`delete` 软删后 `kc.deleteUser`(挡新 SSO 登录);且 `OidcJitUserService` 对**已删除**(`keycloak_id` 有 `mark=0` 行)的 token **拒绝重新 provision**(返回 null,不再插入无角色无编号的新行)。此前删 `sozo-admin2` 后,其未过期 token 一请求就被 JIT 重建成一个 displayName=`sozo-admin2 sozo-admin2`、无编号的新用户。配合前端守卫(无可用菜单→登出),被删用户刷新即登出、后端也不产垃圾行。
- [ ] **SSO 首次登录不会绑到别人的账号上(安全漏洞修复)**:在某租户里造这个局 —— 业务用户 B 的 **email** 是 `bob@example.com`(用户名是别的,比如 `bob`);Keycloak 里新建用户 A,**用户名**就叫 `bob@example.com`(KC 的 `registrationEmailAsUsername` 一开,邮箱形状的用户名就是常态)。让 A 首次 SSO 登录 → 系统必须**为 A 新开一行**,B 的 `keycloak_id` 与 `password_hash` **一个字节都不许动**;A 拿到的业务身份必须是 A 自己的新 ULID,不是 B 的。此前 JIT 的「legacy 绑定」分支调的是 `findByIdentifier` —— 那是**登录框**用的匹配器,SQL 是 `username = ? OR email = ? OR user_no = ?` + `LIMIT 1` 且**无 ORDER BY**,给人在登录框里随便输哪个都能进用的,拿来做「IdP 身份 → 数据行」的机器绑定就错了。命中 B 之后那条 UPDATE 会把 **A 的 `keycloak_id` 写到 B 的行上**,于是从下一个请求起快路径 `findByKeycloakIdAndTenant` 把 A 解析成 **B 的业务用户 id** —— A 直接继承 B 的角色、部门与数据范围;同一条 UPDATE 还会把 B 的 `password_hash` 置空(除非 B 是 SUPER_ADMIN),顺手毁掉 B 的 break-glass 凭据。项目其实早就知道这个匹配器有歧义:`UserAdminService.assertEmailAvailable` 的注释就写着「email 兼作登录标识符(`findByIdentifier` 匹配 `email = ?` … `LIMIT 1`),重复会让邮箱登录解析产生歧义」—— 但只在**登录**那侧设了防,**JIT 绑定**这侧没有。而该分支自己的 javadoc 写的就是「a row exists with the same **(tenant_id, username)**」,库里也确实有 `uk_core_auth_user_tenant_username`(`WHERE mark = 1` 的部分唯一索引),所以精确按用户名查既是本意也无歧义。新增 `UserMapper.findByUsernameAndTenant` 专供绑定使用;登录路径(`AuthService.login` / `AdminAuthController.login`)继续用 `findByIdentifier` 不变。由 `OidcJitUserServiceTest` 新增 2 条钉住(绑定只查精确用户名、绝不碰登录匹配器 / 邮箱撞名时不绑他人而是新开一行,且 `update` 一次都不调用),已实测把那一行换回 `findByIdentifier` 即 14 处失败。
- [ ] **JIT 自动落地保留且数据完整**:真正全新的 KC 用户(无 `mark=0` 旧行)首次 SSO 时仍自动落地一行,且**采番分配 `user_no`**(与 `UserAdminService.create` 同一编号,不再永久无编号——本无"事后补编号"入口)。该用户尚无角色 → 菜单为空 → 前端守卫先登出;管理员在用户列表给其授角色/部门后,再次登录即可正常使用。numbering 失败(租户定义缺失)只记 WARN、不阻断登录(`user_no` 退化为 NULL)。
- [ ] **超管唯一**:在「用户管理」给第二个用户勾选超管角色 → 选择器里根本看不到该角色(非持有者隐藏);即便绕过前端直接调 `assignRoles` 带上超管 roleId,后端返回 `error.user.superAdminSingleton`。唯一超管自身整账号受保护,对其调 `assignRoles`(无论剥离还是改其他角色)→ `error.user.adminProtected`。
- [ ] **封堵等效超管**:新建/编辑自定义角色时,权限选择器看不到 `tenant:*`/`*:*`;绕过前端 `bindPermissions` 带上该权限 id → 后端返回 `error.role.superPermissionReserved`。内置超管角色仍正常显示并持有 `tenant:*`(只读)。
- [ ] **租户管理员账号完全只读**:用其他用户(持 user:update 等用户管理权限)登录,用户列表里超管那行(带「租户管理员」徽标)的操作列**不显示任何按钮**;绕过前端直接调任一 `/admin/user/*` 写接口(update/assign-roles/change-dept/status/delete/reset-password/force-logout)针对超管 → 一律 `error.user.adminProtected`(含改邮箱/显示名)。超管本人在「个人资料」页可改自己的邮箱/显示名。普通用户不受影响。识别用后端下发的 `superAdmin`/`builtin` 标志,不再硬编码用户名。
- [ ] **新租户能建可用的自定义角色**:新建租户后,在该租户「角色管理」新建角色,权限选择器里有完整业务权限(user/role/dept/menu/task… 不含 platform/opsuser);给角色配权限并授予某用户后,该用户登录能看到对应菜单。(回归:此前新租户权限目录只有 `tenant:*` 且被隐藏 → 选择器全空 → 自定义角色零权限 → 该用户登录无菜单。)
- [ ] **新功能权限同步到所有租户**:给某 `@RequiresPermission` 新增一个权限码(或新模块)后重启后端,在**已有**租户(如 demo)的「角色管理」权限选择器里能看到该新权限;新建角色勾选它并授予用户后该用户能访问对应页面。(回归:此前新权限只进 `system` 租户,已有业务租户看不到。)
- [ ] **零权限用户不再死循环**:给一个用户只建空角色(无权限)或不分配角色,其登录后**停在登录页**并提示「账号暂无可访问页面,请联系管理员」(`reason=no-access`),**不会**反复自动 SSO 重登。管理员给其角色配上权限后,重新登录即正常进入。
- [ ] **分配角色会校验角色 id(bug 修复)**:绕过前端直接 `PUT /admin/user/{id}/roles`,body 里塞一个**不存在的**、**已软删的**、或**属于别的租户的** roleId → 返回 `error.user.roleNotFound`(400),且**一条链接都不写**(连"先解绑全部"那步也不执行)。传空数组仍表示"清空全部角色",照常放行。此前完全不校验:不存在的 id 会撞 `fk_core_rbac_user_role_role` 外键 → 500「Unhandled exception」;已软删/跨租户的 id 反而能插入成功(外键只认 `core_rbac_role(id)`,不认租户),链接**不授予任何权限**(权限/菜单/数据范围四张表的 JOIN 全部按租户过滤、fail-closed),但会留在 `core_rbac_user_role` 里变成脏数据。同时 `GET /admin/user/{id}/roles` 改成 JOIN 活跃角色返回,不再把这类悬空链接当成"已勾选"回显(与 `RoleAdminService` 三个 list 方法同一处理)。由 `UserAdminServiceTest` 钉住。
- [ ] **不能在用户管理里操作自己**:登录用户在「用户管理」里看到自己那行时,编辑/删除/停用/强制登出按钮全部置灰(hover 提示去个人资料页);绕过前端直接调任一 `/admin/user/*` 写接口针对自己 → `error.user.selfManagementForbidden`。**自助改资料**:进「个人资料」页改邮箱/显示名 → 保存成功,顶栏显示名随之更新;部门/状态/角色在该页不可改。
- [ ] **用户列表按角色筛选**:用户管理搜索区的「角色」下拉(选项=本租户角色列表)选中后搜索 → 仅列出**持有该角色(有效绑定)**的用户;与关键字/部门条件可叠加;「重置」清空该条件。后端 `GET /admin/user/list?roleId=` 用参数绑定的 EXISTS 子查询过滤(`core_rbac_user_role`,`mark=1`,限本租户)。
- [ ] **改邮箱/显示名同步 KC**:在用户管理编辑某用户(或在个人资料页改自己)的邮箱/显示名 → KC 该用户的 email/姓名同步更新(账号控制台可见),「忘记密码」邮件发往**新**邮箱;KC 拒绝时(如格式违规)前端报错且本地值不变。
- [ ] **编辑改邮箱撞重复被精确拦截**:把某用户邮箱改成同租户另一用户已用的邮箱(用户管理编辑、个人资料页、平台运维用户编辑三处)→ 按语言提示「该邮箱已被占用」,本地与 KC 均不变;改回自己当前邮箱(值不变)不报错。
- [ ] **重置密码(对齐平台侧)**:对普通业务用户点「重置密码」→ confirm(提示当前密码立即失效、会被登出)→ 抽屉显示**一次性临时密码**(可复制)+ 邮件已发/未发提示;被重置用户旧会话下次请求即被拒,用临时密码登录后 KC 强制其自设新密码。自己那行的按钮置灰,超管/内置 admin 行不显示按钮;绕过前端直调 API → `error.user.selfManagementForbidden` / `error.user.adminProtected`。SSO 模式下该按钮**不再整体置灰**(原「SSO 模式下不可用」限制已随手输密码路径一并移除)。
- [ ] 改某角色权限后,持有该角色的用户**下次请求**即生效(权限缓存按角色失效)。
- [ ] 菜单按权限显隐;无权限的路由直接访问被拦/404。
- [ ] 菜单管理列表的类型徽标按 `menu_type` dict 的 css_class 着色:目录=info(蓝)、菜单=success(绿)、按钮=violet(紫)。
- [ ] 菜单管理**操作列**:ops(`*:*`)能看到增删改按钮——写权限码是 `platform:menu:*`,前端 `v-permission` 必须带 `platform:` 前缀(`*:*` 只匹配 `platform:` 命名空间,不匹配 2 段 `menu:*`);业务 super(`tenant:*`)看不到写按钮,但 `menu:read` 仍可用于 RoleEdit 菜单选择器。
- [ ] **菜单父级不能成环(bug 修复,服务端也要挡)**:绕过前端父级选择器直接 `PUT /admin/menu/{id}`,把 `parentId` 设成**自己**或自己的**任一子孙** → 一律 400(`A menu cannot be its own parent` / `New parent is a descendant`);`parentId` 指向不存在的菜单 → 404;`parentId` 传空串(升为根)仍允许。为什么必须服务端挡:菜单是**全局一套**(V41),`MenuQueryService.assembleTree` 只把无父节点收作 root,处在环里的节点挂不到任何 root 上 → **整条分支从所有人的 `/menu/me` 里静默消失**,接口还返回 200、毫无提示。此前只有前端选择器排除了自己+子孙,后端零校验(同门的 `DeptAdminService.update` 一直有这两道校验)。由 `MenuAdminServiceReparentTest` 钉住(含「库里已有脏环时校验不能死循环」)。
- [ ] **内置 admin 同样完全只读**:内置 `demo-admin` 那行与租户超管同等保护——操作列无按钮、所有写接口 `error.user.adminProtected`。判定由后端 `UserDto.View.builtin` 下发(后端 `BUILTIN_ADMIN_USERNAME` 单一来源),不在前端硬编码用户名;后端守卫兜底。
- [ ] 数据范围五模式:ALL=全部;本部门=仅本部门行;本部门+子=含子部门行;仅本人=仅自己创建的行;自定义=角色指定部门(含子树)。多角色**并集**:DEPT+SELF 用户看到 本部门行 ∪ 自己创建的行(用 demo task 模块验证行集差异)。
- [ ] **无部门用户 fail-closed**:`dept_id` 为空的用户只持 本部门/本部门+子 角色 → 列表为空(SQL `1=0`),不是全可见;加 SELF 角色后仅见自己创建的行;CUSTOM/ALL 不受自身部门影响。
- [ ] **无归属数据 fail-closed**:`dept_id=NULL` 或 `create_user='system'` 的行,部门/SELF 范围用户都看不到,仅 ALL 范围/超管可见。
- [ ] **调部门即时生效**:管理员把用户换部门后,该用户**下次请求**即按新部门过滤(`userDataScope` 缓存被即时清除,无需等 5min);旧部门数据默认不再可见,但 SELF 角色下自己创建的行仍可见。
- [ ] **防遗忘切面**:`@DataScope` 标注的方法若忘记 `DataScopeHelper.apply`,dev/test 环境请求直接被拒(prod 记 WARN),不会静默放出全表。
- [ ] **单对象端点也受数据范围约束(IDOR 修复)**:以 DEPT/SELF 范围用户登录,拿同租户**其他部门**的 task id 直接 `GET/PUT/DELETE /demo/task/{id}` → 返回 `NOT_FOUND`(不能越部门读/改/删);本部门内的 id 仍正常。机制:`get/update/delete` 经 `TaskService.loadVisibleOr404` 用 `DataScopeHelper.isVisible` 校验(列表过滤之外,单对象 `selectById` 路径单独把关);跨租户按 id 仍被租户拦截器挡。新业务模块按 backend/AGENTS.md Hard Rule 14 照此实现。
- [ ] **部门改上级时子部门跟着走(bug 修复)**:建 A(有子部门 B,B 下还有 C,把 C 停用),把 **A 改挂到另一个上级**下 → B、C 的 `path` 与 `level` 应当**一并重挂**(`path` 前缀换成 A 的新位置、`level` 同步位移,C 虽然停用也照样跟着);随后以「本部门+子部门」范围的角色看 A → 应当仍然看得到 B、C 的数据。此前重挂只改被移动的那一个节点,子孙的 `path`/`level` 原样不动 —— 代码注释承认了这一点并说交给一个「rebuild paths 管理命令(Stage 5)」,而那个命令**从来没被实现**。后果是树自相矛盾:`parent_id`(管理界面渲染用的)是对的,而 `path`(**数据范围解析真正依据的**)还描述着旧位置。已在真实库上验证:把有子部门的 A 合法重挂后,`DEPT_AND_SUB(A)` 只返回 A 自己(A 的负责人看不到子部门的数据了),而子部门仍挂在旧祖先的子树里 —— 界面看着一切正常,权限却已经错了。现在在**同一事务内**用一条 `UPDATE ... path = 新前缀 || substring(path from 旧前缀长度+1), level = level + 位移` 把整条子树重挂(只按 `mark` 匹配、**不看 status**,理由与 `findSubtreeIdsAnyStatus` 相同:停用的后代必须跟着父节点走)。真实库验证结果:两层后代(含停用那个)全部重挂、各自尾部保留、`level` 同步位移,并且全树不变式 `child.path = parent.path||'/'||child.id AND child.level = parent.level+1` 为**真**。由 `DeptAdminServiceDeleteTest` 钉住 3 条(重挂时用正确的前缀/长度/位移/租户调级联、改名或改排序**不**触发级联、上级没变时不当作移动)。
- [ ] **已停用的子部门不再被树操作漏掉(bug 修复,两个表现)**:先把某部门 C **停用**,C 的父部门是 D。① 强删 D(force) → C **也**被软删、C 里的用户 `dept_id` 被清空、指向 C 的 `role_dept` 绑定也被清掉;不再出现「C 还活着(mark=1)、`parent_id` 指着已被软删的 D」这种孤儿。② 把 D 的上级改成 C → 报「新上级是自己的后代,会形成环」并拒绝;不再真的做成 `D.parent=C` 且 `C.parent=D` 的环(D 的 `path` 里还套着自己的 id)。**同一个根因**:两处结构性操作都用了带 `status = 1` 过滤的 `findSubtreeIds`,而删除前的 IN_USE 计数**不看 status** —— 于是「预检查数到了、要求 force」和「级联真正删到的集合」是两个不同的集合;环检测同理看不见停用后代,而 `require()` 只校验 `mark=1`、照收。两者都已在真实库上验证(级联后 `child_mark=1 / parent_mark=0`;环检测 `detected = f` 后确实写出了 A→B→A)。原先那句「列表为空则回退到自身」的兜底只挡得住**全空**,挡不住**残缺** —— 而停用子部门产生的正是残缺。现在结构性操作走新的 `findSubtreeIdsAnyStatus`(只过滤 `mark`),数据范围解析仍用带 `status=1` 的旧查询(那里「停用即不可见」才是本意),mapper 上写清了这条分工。菜单侧的等价守卫(`MenuAdminService.assertReparentable` 沿 `parent_id` 上溯、只判 `mark`)本来就没有这个缺口。由 `DeptAdminServiceDeleteTest` 钉住 3 条。
- [ ] **停用被 CUSTOM 引用的部门有提示**:把某个被自定义数据范围角色引用的部门停用 → 弹「N 个角色把该部门设为自定义数据范围,停用后这些角色将看不到该部门数据」确认;确认后才停用(force 重发);未被引用的部门停用无打扰;**启用方向**不触发检查。
- [ ] **demo task 页的写操作按钮按权限隐藏(bug 修复)**:用 `docs/data-scope-demo.md` 里的 demo 账号登录看 `/demo/task` 的按钮——`takahashi_shinichi`(京都連絡担当,只有 `task:read`)**看不到**新規/編集/削除;`suzuki_misaki`(一般社員,`task:create`+`read`)只看到新規;`yamada_hanako` / `sato_ken`(无 `task:delete`)看不到削除;超管(`tenant:*`)三个都在。此前这三个按钮没有 `v-permission`,窄权限账号点下去必吃 403。业务模块的写按钮一律用 `v-permission` 门禁(与 Role/Dept/User/Tenant/Menu/PlatformUser/Event 页一致),由 `tests/components/buttonGating.test.js` 钉住;business-demo 是新模块的抄写样板,这里漏掉会被复制出去。
- [ ] **RoleEdit 显示失效的部门绑定**:把某 CUSTOM 角色绑定的部门停用(或手工删)后打开该角色编辑的「数据部门」tab → 顶部出现琥珀色「已失效的部门绑定」区块,列出失效 id,可单个 × 移除或「全部移除」后保存;正常绑定不受影响。
- [ ] **已删除部门的绑定不再放行数据**:绑定指向 `mark=0` 部门的 CUSTOM 条目解析时被跳过(此前会把裸 id 加进可见集,使盖该部门章的行仍可见)。
- [ ] 新建业务用户选**邀请邮件(INVITE)**方式时**不需要密码**:不再报 701 `password 个数必须在8和128之间`(DTO 只 `@Size(max=128)`,不设 min;密码长度/复杂度只在 DIRECT 模式由 `PasswordPolicyService` 校验)。DIRECT 模式空/短密码仍被服务端拒。
- [ ] **分配角色时畸形 `roleIds` 不再 500(bug 修复)**:`PUT /api/admin/user/{id}/roles` 传 `{"roleIds":["R1","R1"]}` → 该角色只绑一次(幂等,不报错);传 `["R1", null, "", "  "]` → 只绑 `R1`;传全空/全 null 的列表 → 等同于清空全部角色。此前**校验看的集合和写入的集合不是同一个**:`assertRolesExist` 内部先去重、又丢掉 null/blank,而下面的 insert 循环遍历的是**原始请求列表** —— 于是三种畸形 body 全都过了校验、然后死在库约束上(均已在真实库验证):`["R1","R1"]` → 「重复键违反唯一约束 uk_core_rbac_user_role」;`["R1",null]` → 「null value in column "role_id" violates not-null」;`["R1",""]` → 「违反外键约束 fk_core_rbac_user_role_role」,三者都冒成 500「Unhandled exception」。`RoleAdminService` 的三个兄弟绑定方法(权限/菜单/数据部门)从来没这个缺口 —— 它们一律先过 `dedupOrEmpty`。现在这里也是**清洗一次**、校验与写入共用同一个 `LinkedHashSet`,`assertRolesExist` 改成只接收已清洗的集合(签名上就杜绝再次分叉)。由 `UserAdminServiceTest` 钉住 4 条(重复折叠 / null+blank 丢弃 / 校验计数按清洗后的集合 / 全空视为清空)。
- [ ] **ULID 引用字段超长时是校验错误、不是 500(bug 修复)**:`POST /api/demo/task` 传 `assigneeUserId` 或 `deptId` 为 27+ 字符 → 得 **701 校验失败**并指名字段;`POST/PUT /api/admin/user`、`PUT /api/admin/user/{id}/dept` 传超长 `deptId` 同理。此前这四条路径都把值**原样 set 到实体**、既不查存在性也没有长度上限(`TaskDto.assigneeUserId` 完全没有 `@Size`,`TaskDto.deptId` 是 `@Size(max=64)`、`UserDto.deptId` 也没有),而这些列全是 `character(26)`:已在真实库上验证 27 字符即被拒(「对于字符类型来说这个值太长了(26)」)→ 冒到 `handleGeneric` 成 500「Unhandled exception」。用户建档那条尤其亏:**Keycloak 用户是在 DB 插入之前建的**,于是一个打错的 deptId 要靠孤儿补偿去回收 KC 用户。现在四处统一 `@Size(max = 26)`(各 DTO 内 `ULID_LEN` 常量,注释里写清为什么是 26)。**注意对比**:菜单/部门的 `parentId` 不需要这条 —— 它们先走 `require(...)` 查存在性,乱值本来就干净地 404(这也是本轮没有把 `@Size` 一把梭加到所有 id 字段的原因)。由 `TaskDtoUlidBoundsTest`(business-demo 首个单测)与 `UserDtoUlidBoundsTest` 钉住。
- [ ] 校验失败(701)前端 toast 显示**具体字段原因**(如 `email: 邮箱格式不正确`),而非笼统的 "Validation failed"——`request.js` 拦截器把 `data` 里的字段错误合进 `msg`。
- [ ] 新建业务用户**用户名/邮箱重复**的提示**按所选语言**显示(不再是英文 prose):服务端在写 KC 前精确预检,抛 i18n KEY(`error.user.usernameExists` / `error.user.emailExists`),前端 `localizeError` 翻译;密码/邮箱必填同理走 KEY。

---

## 2. プラットフォーム運用コンソール（Platform Ops Console）

> 入口均为顶层平台菜单(V48 拍平),仅平台运维可见。

### 2.1 租户管理(Tenant management)
新建(KC realm + 注册行 + 管理员邀请)、停用/恢复、硬删除(回收站式:先停用→输入 code 确认)、重发邀请、发起支持会话。
**列表 UI(重设计)**:页头(eyebrow/标题/副标题)+ 工具栏(实时搜索 + 状态分段筛选 All/运行中/已停用,计数来自 stats + 表格/卡片视图切换,记忆于 localStorage)+ 可折叠/可关闭的回收站提示 + 列表面板 + 分页页脚。每行:字母徽标(按 code 取色,基于 `--card/--foreground` 适配调色板+深色)、双行单元格(显示名 + 等宽 code)、语义状态点(运行中=绿/已停用=琥珀)、**用户数**(该租户下 `core_auth_user` 未删除用户数,后端 `list` 一条分组查询批量算出,内置租户也照算)、相对时间(hover 显示绝对值)、**悬停才出现**的操作图标簇;已停用行有斜纹底纹;**仅** `system` 是内置租户、禁用破坏性操作(`demo` 已是普通 seeded 租户,可编辑/停用/删除)。点击行 → 右侧**详情抽屉**(联系邮箱、KC realm=tenantCode、创建/更新时间 + 操作按钮)。颜色/字体走 app 主题令牌(非设计稿硬编码),设计稿中后端没有的字段(plan/region/owner)已省略。状态分段筛选走**后端** `GET /platform/tenants?status=0|1`(新增可选参数)。
**测试点**
- [ ] 新建租户:KC realm 建好、管理员收到邀请、列表出现该租户。
- [ ] 停用→登录被拒;恢复→可登录。
- [ ] **停用立即终结现有会话(bug 修复)**:租户被停用后,其用户**正在使用的会话**下一个请求即 401——不只是挡新登录。机制:`suspend` 调 `sessionTermination.terminateTenant(code)` 写**租户级 kick**,`ForceLogoutFilter` 按 `RequestContext.tenantId()` 比对 token `iat`(自包含 JWT 不查 realm 实时状态,光禁用 realm 拦不住已签发 token)。停用期间也无法重新登录:OIDC 被禁用的 realm 挡住;password 模式 `AuthService.login/refresh` 加了租户状态校验(`error.auth.tenantSuspended`)。恢复→`reactivateTenant` 清除 kick,可重新登录。
- [ ] 硬删除需先停用 + 输入正确 tenantCode;**仅** `system` 不可改/停/删,也不能被新建占名;`demo` 可改/停/删、其名也可被新建。
- [ ] **硬删除真的清空该租户的全部按租户数据(bug 修复)**:拿一个有活动的租户 X(发过领域事件、有站内通知、有租户级定时任务),停用 → 硬删除 → 逐表核对 `SELECT count(*) FROM <表> WHERE tenant_id='X'` **全部为 0**:`core_domain_event` / `core_notification` / `core_job` / `core_job_log` 也在内。此前 `hardDelete` 的删除清单是手写的、**没有任何东西把它和 schema 绑在一起**,这四张后来才加的按租户表从来没被补进去。残留不是无害的:① `core_domain_event` 由 `OutboxDispatcher` **跨租户**排空,死租户的 pending 行会一直重试到 max-attempts 然后永远停在 `dispatch_state=2`,而 `PlatformDashboardService.reliability()` 统计积压/失败事件时**不带租户谓词**,于是可靠性 KPI 与事件控制台被永久污染(已在真实库确认该表存在按业务租户码的行);② `core_notification` 由 `NotificationService.create` 显式打上业务租户,收件箱行原样留存;③ `core_job` / `core_job_log` —— `DynamicJobScheduler` 每 15s **跨租户**重读所有 enabled 行、并以 `jobCode::tenantId` 为键登记,所以一个已删租户的租户级任务会**永远继续触发**,跑在早已不存在的数据上(目前只有 PLATFORM 行,但作用域模型本就是按租户的,这份清单必须在那之前就是对的)。修法不只是补四行:清单抽成具名常量 `TenantAdminService.TENANT_PURGE_TABLES`,并新增开机守卫 `TenantPurgeCoverageGuard` —— 照 `TenantSchemaGuard` 的路子,从 `information_schema` 取出所有带 `tenant_id` 列的表,与「清单 ∪ 按设计不清理的表」做差集,有遗漏就打醒目 WARN(不 fail boot:漏一张表不影响任何请求,只在极少数运维发起的清理里留残渣,硬失败会挡住「新表迁移先于清理行」的正常开发节奏)。按设计不清理的两张写在 `NOT_PURGED_BY_DESIGN` 里并附理由:`core_tenant`(所有行 `tenant_id='system'`,按 id 在最后单独删)与 `core_support_session`(是平台运维的行为审计而非租户数据,目标租户在 `tenant_code`,仪表盘本来就是 LEFT JOIN)。由 `TenantPurgeCoverageTest` 钉住 5 条(覆盖完整 / 四张漏表已补 / junction 表排在被引用父表之前(FK 是 RESTRICT,顺序反了会中途约束失败) / 注册表不按 tenant_id 删 / **新增一张按租户表会被守卫报出来**),已实测把 `core_domain_event` 从清单里摘掉即 3 条失败。
- [ ] **硬删后同名重建,新租户的超管仍受保护(bug 修复)**:在**同一个后端进程**里,把租户 X 停用→硬删→立刻用**同一个 tenantCode** 重新建一个,然后以别的管理员账号进 X 的用户管理,尝试编辑/停用/删除 X 的超管 → 必须仍然报「管理员受保护」;把超管角色授给第二个人 → 必须仍然被拒。此前 `BuiltInRoleLookup` 把「租户 → SUPER_ADMIN 角色 id」缓存 10 分钟,而 `invalidate()` **一个调用者都没有**;类注释还把这事写成自愈的(「create 是自然 miss;hard delete 后该租户已无调用者」)—— 两半都不成立,因为硬删后**同名重建是受支持的流程**(`uk_core_tenant_code` 是 `mark=1` 的部分唯一索引),重建时 `RbacSeederService` 插的是一条**全新 ULID** 的角色。已在真实库上验证:删前 id 与重建后 id 不同,且用**旧 id** 查 `existsActiveLink` 返回 0 行 —— 于是 `isTenantSuperAdmin()` 判否,连锁后果是:`assertNotProtectedAdmin` 不再认这个人(任何持 `user:update` 的账号都能编辑/停用/删除/改角色/踢下线/重置他)、`assignRoles` 的「超管唯一」守卫比对的是过期 id(能把超管授给第二个人)、break-glass 豁免也不认他(首次 SSO 绑定会清掉他的 `password_hash`)。窗口是 10 分钟(Caffeine `expireAfterWrite`),且只在服务过该租户的那个进程里出现 —— 正好覆盖「建错了马上重建」这种最常见的操作。现在 `hardDelete` 与 `create` 两侧都调 `roleLookup.invalidate(tenantCode)`(create 侧针对**负结果也被缓存**这一半:loader 特意用 `Optional` 包装,好让 Caffeine 连「查不到」一起缓存)。由 `TenantAdminServiceTest` 钉住 2 条。
- [ ] **命令行建 realm 的脚本与平台控制台走同一套处理(bug 修复)**:跑 `infra/keycloak/new-tenant.sh acme "Acme Inc."`(或 `new-tenant.ps1 -Name acme -DisplayName "Acme Inc."`)生成 `realms/acme-realm.json` → 该文件里**不应有任何 UUID 与 `demo-realm.json` 相同**,`displayName` 应为传入的显示名(不再是 "Demo Tenant"),`realm` / `claim.value` / `/realms/acme/account/*` / `/admin/acme/console/*` 均已改写;重启 Keycloak(`--import-realm`)后该 realm 能正常导入、账号控制台可用。此前两个脚本只做了 4 处字符串替换,**漏了 UUID 重生成和 displayName** —— 而 JVM 侧的 `KeycloakRealmService.renderRealmJson` 自己的注释就写明了后果:「模板是 demo realm 的**导出**文件,带着 demo 的 realm id 和每一个嵌套 role/client/mapper id,这些在 Keycloak 里是**全局唯一主键**;照抄会和已存在的 demo realm 撞车,`kc.realms().create()` 返回 409」。已实测:修复前脚本产出的文件与 `demo-realm.json` 共享**全部 129 个 UUID**(含 realm 自身主键 `946f39a6-…`)、`displayName` 仍是 "Demo Tenant";修复后共享数为 **0**、映射一致(同一个源 id 在文件各处映射到同一个新 id,内部引用不断)、JSON 仍合法。两个脚本的注释与 README 也一并对齐,并注明必须与 `renderRealmJson` 保持同步。
- [ ] 新建租户的 numbering 定义从预留模板 `__template__` 克隆(已与 `demo` 解耦);即使删掉 `demo`,新建租户首次取号(U 编号)仍正常。
- [ ] **新建租户的 KC 账号控制台可用(bug 修复)**:新租户用户在右上角「修改密码」打开 KC 账号控制台 → 正常进入,**不报「无效的参数: redirect_uri」**。realm 克隆(`KeycloakRealmService.renderRealmJson` 及 `infra/keycloak/new-tenant.{ps1,sh}`)现在会把内置客户端 URL 一并替换为新 realm 名(`account`/`account-console` 的 baseUrl + `/realms/<code>/account/*` redirectUris、`security-admin-console` 的 `/admin/<code>/console/*`);此前停留在 `/realms/demo/...`,克隆出的每个租户 realm 都拒绝自己的账号控制台跳转。存量坏 realm 用 KC Admin API 改这三个客户端的 baseUrl/redirectUris 即可修复。
- [ ] 重发邀请(可改邮箱)。
- [ ] **「重发管理员邀请」重发的是管理员、不是最近被邀请的人(bug 修复)**:新建租户 A(管理员收到邀请、**先不激活**),再进 A 的用户管理用**邀请方式**新建一个普通业务用户(也不激活),然后回平台控制台点 A 的「重发邀请」→ 收到新链接的必须是**管理员**;那个普通用户的待用链接**不受影响**;若同时填了「修正邮箱」,被改的是管理员的邮箱和 `core_tenant.contact_email`,**不是**那个普通用户的。此前挑人的方式是「该租户所有未使用邀请里 `expires_at` 最大的一条」—— 而业务用户的邀请(`UserAdminService` INVITE 方式)落在**同一张 `core_user_invite`、同一个 `tenant_id`**,两条路径又共用 `app.invite.token-ttl`,所以「`expires_at` 最大」等价于「最近被邀请的人」,几乎永远不是建租时就被邀请的管理员。已在真实库上验证:摆一条 admin 邀请 + 一条更晚的业务用户邀请,旧查询挑中的是业务用户(`is_the_tenant_admin = f`)。后果是运维看到「已重发」的成功提示,而实际发生的是:那个员工的待用链接被作废并**以租户管理员的邮件模板**重新寄给他;若填了修正邮箱,他的邮箱在 DB 和 Keycloak 里都被改掉、还被写进 `core_tenant.contact_email`(租户的平台联系人变成一个普通员工的邮箱);而管理员的邀请从头到尾没被碰过 —— 运维真正想做的那件事没发生。现在按「持有该租户内置 SUPER_ADMIN 角色的人」定位(`assignRoles` 禁止把超管授给第二个人,所以持有者唯一;用 `is_built_in = 1` 判定,不依赖角色名)。**同时补上内置租户守卫**:`system` 持有 PLATFORM_ADMIN/OPERATOR 两个内置角色、也存着运维用户自己的待用邀请,单靠角色 JOIN 仍会挑中运维用户 —— 该操作对 `system` 直接拒绝(重发运维邀请走平台用户控制台)。前端本来就把这个按钮对 `system` 置灰,但端点才是真正的边界。由 `TenantAdminServiceTest` 钉住 2 条。
- [ ] 状态分段(运行中/已停用)服务端过滤正确、跨分页一致;计数与列表吻合。
- [ ] 实时搜索(去抖)、表格/卡片切换(刷新后记忆)、详情抽屉(Esc/遮罩/X 关闭)。
- [ ] 表格/卡片均显示**用户数**;新建用户/删除用户后该数随之变化;system 显示运维用户数、demo 显示其 seed 用户数。
- [ ] 行操作权限门控与原表一致;悬停显示;仅 `system` 禁用编辑/停用/删除/重发(禁用 tooltip 文案只提 `system`,不再提 `demo`),`demo` 与普通租户一致。
- [ ] 切换调色板 + 深色模式:徽标、状态点、抽屉、分页样式随主题正常显示。

### 2.2 平台总览(Platform overview / Ops dashboard)
独立菜单页 `/platform/overview`(菜单 `platform.overview`,门控 `platform:tenant:read`,排在租户管理之前)。按 Claude Design 稿重做成 **bento 网格仪表盘**:顶部「最后更新 + 刷新」;4 张 KPI 卡(图标在左 + 大数字 + delta:总租户/运行中/已停用/本月新增,数据 `/platform/tenants/stats`);状态环形图(圆心放总数,图例运行中/已停用 + 占比);按月新增趋势(**手绘 SVG 面积线,直线段不过冲**);入驻漏斗(tiles + 激活率条);活跃与参与(tiles + 14天登录趋势 + 沉默租户列表);平台健康(tiles + 全部正常/需关注 pill);安全与高权限监视(tiles + 审计列表:支持会话/应急访问,严重度色点 + 字母徽标 + actor→target + 相对时间),数据 `/platform/dashboard`。**保留原有点选切换明细功能**:入驻(待激活/已过期)、平台健康(任务失败/事件积压/最久积压/接口错误)、安全(支持会话/应急访问)的 tile 可点击,切换其下明细列表,默认选中第一个有值的 tile。**平台健康明细每行末尾有小眼睛**,点击弹出抽屉显示该行全部字段(错误信息不截断)。入驻漏斗等客户指标只排除 `system`(平台内部;含平台运维用户的邀请链接);`demo` 作为普通(seeded)客户租户**计入**所有指标。颜色走 app 主题令牌(适配强调色+深色);图表为自建组件 `AreaChart.vue`(已移除 echarts 与旧 `DashboardPanels.vue`)。**与 2.1 租户管理分页**:dashboard 独立成页,租户表格首屏即可见。
**测试点**
- [ ] 「平台总览」作为独立菜单出现(在「租户管理」之前);ops 与 operator 都能看到。
- [ ] KPI/图表/tiles 显示真实聚合(非 mock);切换强调色+深色模式整页配色正常。
- [ ] 环形图圆心显示总租户数;趋势/登录折线为**直线段**,近 0 数据不出现假驼峰、不下探基线。
- [ ] 平台健康全为 0 → 绿色「一切正常」pill;有任一 >0 → 黄色「需关注」pill。
- [ ] 安全审计列表:支持会话(进行中=红点/历史=黄点)+ 应急访问(火苗图标),相对时间,空时显示「暂无」。
- [ ] **点选切换明细**:入驻/平台健康/安全的 tile 点击切换其下明细列表(待激活↔已过期、任务失败↔积压↔接口错误、支持会话↔应急访问),默认选中第一个有值 tile,选中 tile 高亮主色边框。
- [ ] 活跃与参与卡显示沉默租户列表;明细列表为空显示「暂无」。
- [ ] 平台健康每条明细行末尾小眼睛,点击弹抽屉显示全部字段(任务/事件/接口错误对应不同字段,错误信息完整不截断)。
- [ ] 入驻漏斗「待激活/已过期」**不含** `system`/`demo`:新建平台运维用户(邀请链接未激活)不会让 `system` 出现在待激活租户里。
- [ ] **入驻漏斗只算租户管理员的邀请、不算普通员工的(bug 修复)**:拿一个**已经在用**的租户(有成功登录、管理员早已激活),进它的用户管理用**邀请方式**新建一个普通业务用户(不激活)→ 平台总览的「待激活」**不应 +1**,下钻明细里也不该出现这个租户;再让该租户的**管理员**处于未激活状态(重发邀请后不点)→ 这时才应 +1、明细里出现一行。此前四条漏斗查询(待激活计数、已过期计数、两个下钻列表)都是「该租户任意未使用的邀请」,而业务用户的邀请落在**同一张 `core_user_invite`、同一个 `tenant_id`**,行上没有任何字段能区分是哪条流程发的。后果是仪表盘**自相矛盾**:已在真实库上验证,只要给 `demo`(14 次成功登录、因此被算作**已激活**)插一条员工邀请,它同时出现在「待激活」里;而下钻那一行只带**租户**的 code / 名称 / 联系邮箱,读起来跟「这家客户从没激活过」一模一样 —— 运维会去追一个早就上线的客户。现在四条查询统一 JOIN 到「持有该租户内置 SUPER_ADMIN 角色的人」(与「重发管理员邀请」同一判据;`assignRoles` 禁止授超管给第二人,持有者唯一;`is_built_in = 1` 判定,不依赖角色名),两跳 JOIN 都带 `tenant_id`(`core_rbac_user_role` 的外键只指向 `core_rbac_role(id)`、不含租户)。修正后同一份数据:只有员工邀请 → 0;补上管理员邀请 → 1 且明细只列管理员那条。基于登录日志的「激活率/中位激活时长」不受影响。由 `PlatformDashboardActivationTest` 钉住 4 条。
- [ ] **KPI 数字和它点开的明细列表必须是同一个时间窗(bug 修复)**:造一条 **25 小时前**失败的定时任务执行记录(或等一条 24h 前的失败自然滑出窗口)→「平台健康」的「任务失败(24h)」tile 显示 **0**,点它展开的明细列表也必须**是空的**,而不是仍列着那条三天前的失败;同理「安全与高权限监视」的「应急访问(7天)」tile 与「支持会话(7天)」tile,点开的列表也不得出现 7 天以前的行。此前三条下钻查询(`recentJobFailures` / `recentBreakGlass` / `recentSupportSessions`)**都没有时间条件**,而它们各自的 KPI 计数分别是 24h / 7d / 7d —— tile 说「0」,点下去却有内容,运维会去追一个早已过去的故障。这不是新规矩:同一个方法里的 `recentOplogErrors` 早就写着「bounded to the same 24h window so the list and the count agree」,只是另外三条没跟上。已在**真实库的回滚事务**里验证:插入一条 `start_time = now() - 3 days, status=3` 的 `core_job_log` 行后,KPI 查询返回 0、下钻查询返回 1 行(事务 ROLLBACK,库无残留)。修复即给三条查询补上与各自 KPI 相同的窗口;支持会话列表同时服务「会话进行中」tile(前端按 `active` 标志过滤),而支持会话 token TTL 只有 30 分钟,7 天的窗口不可能漏掉一个进行中的会话。事件积压列表**故意不加窗口**(它的 KPI 本身就不带时间条件,越老的卡住事件越要看见)。由 `PlatformDashboardDrilldownWindowTest` 钉住 5 条(三条补窗口 + `recentOplogErrors` 不回退 + 事件积压不许被误加窗口),已实测撤掉任一处修复即对应用例失败。
- [ ] 点「刷新」重新拉取 stats + dashboard 并更新「最后更新」时间。

### 2.3 领域事件控制台(Domain events)
`/platform/events`:分页列表(按分发状态/类型/关键字筛)、详情(payload)、失败事件**重发**(单条/批量,仅 `dispatch_state=2`)。
**测试点**
- [ ] 增删改 demo task → 产生 `demo.task.*` 事件并出现在列表(几秒内 待分发→已分发)。
- [ ] 按"失败"筛选;对失败事件点重发 → 状态回到待分发并被重新分发。
- [ ] 查看详情能看到 JSON payload。
- [ ] 非平台运维访问 → 看不到菜单 / API 403。

### 2.4 定时任务(Scheduled jobs)
`ScheduledJob` SPI → `core_job`,管理台可启停/改 cron/立即执行/看日志。删类重启 → 配置行软删下线。Outbox 保留作业(`core:outbox-retention`,每天 **JST** 03:30,删已分发的旧事件,V49/V50)。cron 表达式一律按**业务时区**(`AppTime.zone()`,部署级配置 `app.timezone`/`CORE_TIMEZONE`,默认 Asia/Tokyo)解释:`CronTrigger` 与「下次执行时间」计算均显式带该时区,与部署机器的系统时区无关。
**测试点**
- [ ] 新建的 `ScheduledJob` 重启后出现在管理台并按 cron 执行。
- [ ] 启停/改 cron/立即执行生效;执行日志可见。
- [ ] 删除 job 类 + 重启 → 管理台不再显示(`core_job` 软删 mark=0,日志保留)。
- [ ] 保留作业只删 `dispatch_state=1` 的旧事件,pending/failed 不动。
- [ ] 「下次执行时间」与执行日志的开始时间按 **JST** 显示(走 `toJSTDateTimeFullDisp`,与领域事件/平台总览一致,不再显示 UTC 裸字符串)。
- [ ] **写执行日志失败时不会把任务锁卡死(bug 修复)**:把 `core_job_log` 临时改成不可写(例如 `REVOKE INSERT`,或制造一次连接中断)→ 触发一个 `concurrent=0` 的任务 → ① 应用只打一条 WARN,调度线程不死、下一个 cron 周期照常再试;② `core_job_lock` 里那把锁**当场释放**(不是等 `max_run_seconds`(默认 300s)租约到期);③ `core_job.last_status` 记为失败,运维在管理台看得见;④ 恢复权限后立刻能正常执行。此前 RUNNING 日志的 insert 写在 `try` **外面**,它一抛异常整个 `finally` 都不执行:锁一直被持有到租约过期,期间所有节点静默跳过这个任务;更糟的是那条 `tenant='system'` 的 `RequestContext` 也留在了**线程池线程**上 —— `system` 正是 `MybatisPlusConfig.ignoreTable` 的租户隔离**旁路信号**,而同一个池还要跑「立即执行」。(项目自己在 `DynamicJobScheduler.loadEnabled` 那轮就是为同一个原因改成快照+还原的。)现在 insert 移进 `try`,`logId` 为 null 就只跳过「结束日志」这一步;`finally` 里两处记账写各自独立 try/catch(否则库还没恢复时它们自己抛出来,又会连带吃掉锁释放和上下文清理)。由 `JobExecutionWrapperFailurePathTest` 钉住 3 条(insert 失败仍释放锁 + 清上下文 / 仍写 `core_job.last_*` / 正常执行路径不受影响),已实测把 insert 移回 `try` 外即 2 条报错。
- [ ] cron `0 30 3 * * *` 的任务在 **JST 03:30** 触发(而非部署机器时区的 03:30);「下次执行时间」与实际触发一致。**在系统时区≠业务时区的机器上验(本机 OS 就是 UTC)**:`core:outbox-retention` 那行的「下次执行时间」换算到 JST 必须读作 03:30。此前该列用 `CronExpression.parse(cron).next(OffsetDateTime.now())` 计算 —— 按 **JVM 默认时区**解释 cron 字段,在 UTC 机器上渲染成 03:30 UTC(= JST 12:30),与实际触发差 9 小时,而且肉眼看不出是错的;而专为此写的时区感知辅助方法 `DynamicJobScheduler.nextFireTime()` **一个调用者都没有**。现已改为优先走该辅助方法(`app.scheduler.enabled=false` 无该 bean 时退回同样带 `AppTime.zone()` 的计算)。由 `JobAdminServiceTest` 钉住(断言写成"换算到业务时区后的壁钟读数",这样只有在两个时区不同的机器上才有意义、不会退化成同义反复)。

### 2.5 支持会话 / 模拟登录(Support session / impersonation)
ops 以目标租户 SUPER_ADMIN 身份操作 30 分钟(`tenant.impersonate.start`);服务端会话表 `core_support_session`(V52)记录 started/expires/**ended_at**。退出调 `/support-session/terminate` 置 ended。
**测试点**
- [ ] 发起支持会话 → 顶部红色横幅 + 倒计时;能以租户身份操作。
- [ ] 安全面板「支持会话进行中」= 真正在线数;退出后立刻归零(不再卡 30 分钟)。
- [ ] 最近会话列表:在线绿点 / 已结束灰点。
- [ ] **仅** `system` 不可被模拟(无业务 SUPER_ADMIN);`demo` 作为普通租户可被模拟。

### 2.6 運用ユーザー管理 / 平台用户(Platform users,两级)
`/platform/users`(`opsuser:*`,仅 ops 可见):列表、新建(KC system realm 用户 + 授 **PLATFORM_OPERATOR**;**与「重发邀请」完全相同的逻辑(plan B)**——KC 用户**不设临时密码**,`mint` 一次性 `/invite/{token}` 链接 + 发 `user-invite` 邮件,用户在落地页自助设永久密码;**页面不弹临时密码、仅 toast**(邀请已发/失败);**走应用 `CORE_MAIL_*`,不依赖 KC realm SMTP**;邮件失败不影响创建,可用「重发邮件」补发)、**编辑(改显示名/邮箱,用户名不可改;同步 KC `updateProfile` + `core_auth_user`)**、停用/启用、**重置密码(轮换临时密码 → 当前密码立即失效;弹窗显示新临时密码作后备 + 发「密码重置」邮件,该邮件**仅含临时密码**;confirm 提示会重置当前密码;并**踢下线**——已登录会话的旧 token 立即失效、需重登)**、**重发邀请邮件(plan B:`mint` 一次性 `/invite/{token}` 链接 + 发 `user-invite` 邮件,用户在落地页**自助设永久密码**;不动当前密码、页面不弹临时密码仅 toast;先作废该用户旧的未用 invite,只最新链接有效)**、**踢下线(ForceLogout:使现有会话立即失效、需重新登录;账号不停用,可立即重登)**、删除。编辑/重置/重发/踢下线走 `opsuser:update`。登录链接指向应用 `/login`(走 SSO 到 KC,不直接暴露 KC UI)。常规运维(PLATFORM_OPERATOR=`*:*`)**看不到本页**。
**测试点**
- [ ] ops 能看到「运维用户」菜单;常规运维**看不到**(`opsuser:*` 不被 `*:*` 覆盖),直接访问 API 403。
- [ ] 新建运维用户**用户名 3-64 位 + 格式**由**前端**先校验(`isValidUsername`,提交前 toast **按语言**显示 `platform.user.message.invalidUsername`),`op` 等不合规直接挡下、不发请求。后端为兜底:DTO `@Size(min=3)`/`@Pattern`(对齐 KC user-profile 长度策略),且任何 KC 拒绝由 `GlobalExceptionHandler` 兜成业务错误(`error.keycloak.operationFailed`,按语言显示)+ WARN 日志,而非「Unhandled exception」500。
- [ ] **业务租户超管(`tenant:*`)看不到「运维用户」菜单、列表/详情接口 403**(回归:`opsuser:` 在 `platform:` 之外,曾被 `tenant:*` 的"非 platform 即业务"规则误吃,导致租户管理员能看到 ops 用户)。
- [ ] 新建用户 → toast「邀请邮件已发送至 {email}」(不弹临时密码);新用户点邮件链接落地页自助设密、再登录。
- [ ] 新用户能管租户/事件等,但**看不到运维用户菜单**(只比 ops 低一档)。
- [ ] 停用→该用户无法登录;启用→恢复;重置密码→弹新临时密码;删除→DB 软删 + KC 用户删除。
- [ ] 不能对**自己**或**ops(超级)**执行停用/删除/重置(行内按钮隐藏 + 后端拦截)。
- [ ] 停用/删除会**立即踢下线**(ForceLogout):被停用用户**当前已登录的会话**下次请求即被拒(不只是阻止再次登录);启用会清除踢出标记。
- [ ] 新建表单必填项 `*` 为红色;邮箱有格式校验(前端正则 + 后端 `@Email`),格式不对被拦。
- [ ] 新建重复检测**精确**:用户名已存在 → 「用户名已被占用」;邮箱已存在 → 「该邮箱已被占用」(在写 Keycloak 之前分别预检,不再把邮箱冲突笼统报成用户名重复)。
- [ ] 新建用户邮箱**必填**;首登**不**经过"更新账户信息"页(不报"请指定此字段"),直接进设置密码——KC 用户的 email/firstName/lastName 创建时已补全(单段名 lastName 也填)。
- [ ] 状态徽标有颜色区分:启用=绿(success)、禁用=红(danger)。
- [ ] 编辑:改某用户的显示名/邮箱 → 列表即时更新;用户名为只读(改不了,提示删重建);邮箱格式校验;Keycloak 用户的 email/firstName/lastName 同步更新。
- [ ] 新建(plan B,与重发相同):创建成功 → **toast「邀请邮件已发送至 {email}」**(不弹临时密码);用户收到 `user-invite` 邮件 → 点 `/invite/{token}` 落地页**自助设永久密码** → 登录。邮件发送失败 → toast「已创建但邮件失败,请重发」,且**创建本身不失败**(`CORE_MAIL_*` 未配时同理)。
- [ ] **新建的两条 DB 写是一个事务(bug 修复)**:`core_auth_user` 行与 `core_rbac_user_role`(PLATFORM_OPERATOR)绑定同生共死。制造第二条 INSERT 失败(如临时改坏 `PLATFORM_OPERATOR_ID` 的 FK 目标)→ 整体回滚 + KC 用户被补偿删除,列表里**不留**半成品行,同名重建可直接成功。此前两条是各自 autocommit:user_role 失败会留下一条无角色、`keycloak_id` 指向已删 KC 用户的行,其用户名还会卡住后续所有重建(usernameDup 预检)。机制:DB 部分收在 `@Transactional persistNewOpsUser`,经 self-proxy 调用(与 `TenantAdminService.persistNewTenant` 同一套路);邀请邮件在事务提交后才发。由 `PlatformUserAdminServiceTest` 钉住。
- [ ] **重置密码**:confirm「会重置当前密码」→ 轮换临时密码(原密码立即失效)+ 弹窗显示新临时密码(后备)+ 发「密码重置」邮件(**仅含临时密码**,无登录地址/用户名/租户行)+ **踢下线**(已登录会话下次请求被拒、需重登)。走应用 `CORE_MAIL_*`。
- [ ] **重发邀请邮件**(plan B):点重发 → 用户收到 `user-invite` 邮件,点 `/invite/{token}` 落地页**自助设永久密码** → 可登录;页面**不弹**临时密码(仅 toast);**不改当前密码**(用户完成链接前仍可用旧密码)。
- [ ] **踢下线**(独立操作):点踢下线 → confirm → ① 应用层 `kickOut` 使已签发 token 失效(下次请求被拒)② KC `logoutUser` 结束其 SSO 会话。跳 `/login` 后**不会被 SSO 静默重登**(KC 会话已结束 → 要求重新输密码);账号不停用,输密码后可重登。**重置密码同样触发**(kickOut + KC logout),否则旧 KC 会话会让 SSO 自动重登、踢下线无效。
- [ ] **运维用户列表的分页参数边界(bug 修复)**:`GET /platform/users?page=0`、`?size=-5` 应正常返回首页而不是 500;`?size=1000000` 应被压到 **500 条**(与其它列表一致)。此前该方法是全项目**唯一手写分页**的地方(`offset = (page-1)*size`),而 controller 只声明了 `@RequestParam(defaultValue=...)`、没有任何校验:`page=0` → `OFFSET -20`、`size=-5` → `LIMIT -5`,Postgres 两者都直接报错(已在真实库上验证:「OFFSET后的值不能是负数」/「LIMIT后的值不能为负数」)→ 冒到 `handleGeneric` 成 500「Unhandled exception」(dev 还会连栈细节一起吐出去);`size` 也完全没有上限,而其余列表都走 MyBatis-Plus、由 `PaginationInnerInterceptor.setMaxLimit(500)` 自动归一化 page 并封顶。现已在服务层按 MP 的同一套语义夹取(page≥1、size∈[1,500])。由 `PlatformUserAdminServiceTest` 钉住 4 个边界。
- [ ] **并发编辑同一条记录不再"报成功但丢改动"(bug 修复)**:两个浏览器窗口(或两位管理员)同时打开同一个角色/菜单/部门/字典项/用户/task 的编辑抽屉,先在 A 保存成功,再在 B 保存 → B 应当收到「该记录已被他人修改,请刷新后重试」(错误码 **702**),而不是提示保存成功却什么都没改。此前这套保护**声明了三处、接线为零**:`MybatisPlusConfig` 装了 `OptimisticLockerInnerInterceptor`、`BaseEntity.update_time` 标了 `@Version`(`NotificationService` 注释还写着 "guards races")、`GlobalExceptionHandler` 也把 702 映射好了 —— 但 ① MP 的拦截器跑在 `beforeUpdate`,**在语句执行之前**,结构上不可能知道会不会命中行(它那个 opt-in 的 `setException(...)` 管的是版本字段缺失,项目用的还是无参构造);② 它从不抛 Spring 的 `OptimisticLockingFailureException`,所以那个 handler 是死代码;③ 全项目**没有任何一处**检查 `updateById` 的影响行数(grep 零命中)。已在真实库上验证:带旧版本的 UPDATE 返回 `UPDATE 0` 且**不报任何错**,B 的改动就这么没了。现在这 10 个由人驱动的编辑路径统一走 `ConcurrentEdit.requireApplied(mapper.updateById(e))`;0 行在这些路径上语义无歧义 —— 调用方都先 `require(id)` 载过行(行必存在),而 MP 总会把 `update_time` 放进 SET 子句(命中必为 1 行),所以 0 只可能是版本被推进了。seeder/出箱/通知已读等单写者或幂等路径不动。5 语言补了 `error.common.concurrentEdit`。由 `ConcurrentEditTest` 钉住 4 条。
- [ ] **角色编辑保存失败不再报成功(bug 修复)**:打开某角色的编辑抽屉,在**另一个窗口把该角色删掉**,回来点保存 → 应弹出后端的错误提示且抽屉**不关闭**,不再显示「保存成功」。此前 `RoleEdit.save()` 里三个绑定调用(权限/菜单/数据部门)的返回码完全不检查:`request.js` 只对 `code === 401` 和 `code === 700` reject,`NOT_FOUND`(404)、`VALIDATION_FAILED`(701)、`IN_USE`(703) 都会**正常 resolve**,于是绑定失败也照样走到 `toast.success` + 关抽屉,用户以为存上了、实际什么都没绑。同一个函数里的新建/更新调用本来就检查 `code !== 0` —— 只有这三个漏了。
- [ ] **邮件里写的有效期必须跟着配置走(bug 修复)**:把 `app.invite.token-ttl` 改成 `3d`、`app.password-reset.token-ttl` 改成 `2d`,然后分别触发「新建业务用户(INVITE)」和「SSO→密码反向迁移」→ 两封邮件正文写的天数应为 **3** 和 **2**,不再是固定的 7。此前这两处硬编码 `"7"`,而同样发邀请的另外两处(`TenantAdminService` / `PlatformUserAdminService`)都是取 `inviteTokenService.ttlDays()`、并在注释里专门写了"绝不能用字面量,否则 TTL 改了文案就不再真实"。后果是收件人按邮件里说的期限去处理,链接却提前失效。顺带给 `PasswordResetTokenService` 补上了它兄弟早就有的 `ttlDays()`(同样的 7 天兜底 + 下限 1 天,避免 sub-day TTL 显示成"0 天")。由 `SsoToPasswordMigrationServiceTest` 钉住(把 TTL stub 成 3 天,断言文案为 "3" 而非 "7")。
- [ ] **新建业务用户失败时不留 Keycloak 孤儿(bug 修复)**:让 DB 插入失败(例如临时把 `core_auth_user` 的邮箱唯一索引撞上),新建用户 → 整体回滚,**Keycloak 里那个用户也被删掉**,同名重建可直接成功。此前只有 `TenantAdminService.create`(删 realm)和 `PlatformUserAdminService.create`(删 KC 用户)有补偿,业务用户控制台这条 —— 三条里最常用的 —— **没有**:KC 先建、DB 后插,插入失败时 `@Transactional` 回滚掉 DB,KC 用户却留下成孤儿。之后运维在 UI 上永远重建不了:DB 预检通过(确实没有行),但 KC 返回 409 → 报「Keycloak user already exists (username or email already in use)」,而这个用户在应用里根本不存在。补偿失败时按 `TenantAdminService` 同样的姿态双 ERROR 记两条因果,供人工清理。由 `UserAdminServiceTest` 钉住。
- [ ] **解锁不再顺手改启用状态(越权 bug 修复)**:先把用户 X 停用(`status=0` → KC 用户被禁用 + 已踢下线),再用只持 `auth:unlock` 的账号调 `POST /admin/auth/unlock {username: X}` → **只清锁定,X 仍是停用状态**,`core_auth_user.status` 不变。此前该端点会无条件 `setStatus(1)`:锁定状态其实全在 Redis(`AccountLockoutService` 只写 `auth:fail:*`/`auth:lock:*`,没有任何代码因登录失败去改 `status`;`AuthService.login` 也把"锁定"与"停用"当两条独立拒绝 `ACCOUNT_LOCKED`/`ACCOUNT_DISABLED`),所以那行 `setStatus(1)` 唯一能改到的就是**管理员刻意停用的账号** —— 而且绕过了拥有该字段的全部守卫:没有 `assertNotProtectedAdmin`(能把内置 admin / 租户超管重新启用,这是别处一律拒绝的)、没有 `assertNotSelf`、**也没有 `SessionTerminationService.applyEnabled`**,于是 DB 显示已启用而 Keycloak 仍拒绝登录、踢出标记也没清 —— 正是 `SessionTerminationService` 当初被引入来消除的 DB/KC 漂移。重新启用账号请走 `PUT /admin/user/{id}/status`(`user:update`),不是窄权限 `auth:unlock` 的副作用。由 `AdminAuthControllerUnlockTest` 钉住。
- [ ] **反向迁移(SSO→密码)不会被一次 SSO 登录悄悄撤销(bug 修复)**:走完 `/reset-password/{token}` 设好本地密码后,**把该用户在 Keycloak 里的账号重新 enable**(模拟"设密时 KC 短暂不可达、禁用没落地",或运维事后误启用而非删除),再用它做一次 SSO 登录 → 应当**进不来**(前端登出、后端一条 WARN 指明该账号已完成 SSO→密码反向迁移、KC 用户需要清理),并且**刚设的密码还在**、仍可用密码登录。此前这条链路是:设密那步清掉 `keycloak_id` 并禁用 KC 用户,但**禁用是 best-effort**(注释原文:「if KC is unreachable the local password is already written ... we just log the orphan KC user for the operator to clean up later」)。于是残留/被重新启用的 KC 账号仍能完成 SSO 登录,而 `OidcJitUserService` 三条分支依次:快路径查不到(`keycloak_id` 已为 NULL)、已删除探测查不到(`mark=1`)、**legacy 绑定分支按用户名匹配到同一个人**,于是重写 `keycloak_id` **并把刚设的 `password_hash` 置 NULL** —— 一个文档写明"不可逆"的操作被静默撤销,而且此人**两边都登不进去**:重置令牌是一次性的、已经花掉了,只能找管理员重跑迁移。修法是在 legacy 绑定分支前加一道判据:`core_password_reset_token` 的行**只由** `SsoToPasswordMigrationService` 铸造(全项目唯一的 `mint` 调用点),所以"该用户存在已消费的重置令牌"是"此账号已刻意迁出 SSO"的无歧义标记 —— 命中即拒绝绑定。守卫**不影响**共用同一分支的正常"密码→SSO"迁移(那类用户没有已消费的反向迁移令牌)。由 `OidcJitUserServiceTest` 钉住 2 条(已迁移者被拒且**不产生 bind UPDATE**、普通 legacy 用户照常绑定)。
- [ ] **两个 pre-auth 设密端点也要留审计行(bug 修复)**:走完 `/invite/{token}` 设密 → `core_oplog` 出现一行 `system / auth.inviteAccept`;走完 `/reset-password/{token}` 设密 → 出现一行 `system / auth.passwordResetAccept`。两行的 `tenant_id` / `user_id` 必须来自**被消费的 token**(不是请求头 `X-Tenant-Id` —— 那是调用者随便填的),`request_body` 必须为空(新密码不得进审计)。此前这两个端点**完全没有审计**,而项目里其它每一条改凭据的路径都有(`auth.breakGlassSet`、两个控制台的 `reset-password`、`auth.unlock`、`auth.forceLogout`);偏偏这两个是**无会话、仅凭邮件里的 token 就能设永久密码**的,`/reset-password` 还会不可逆地摘掉 Keycloak 关联。不能简单加 `@OpLog`:切面读 `RequestContext`,pre-auth 时用户为空、租户取自请求头 → 写出错误归属的审计行,比没有更糟;故照 `AuthService.recordBreakGlassUse` 显式构造。审计失败绝不影响用户设密。由 `PreAuthCredentialAuditTest` 钉住。
- [ ] **Keycloak 里的超长用户名不再让人「登得进却处处 500」(bug 修复)**:在 Keycloak 管理台给某租户 realm 直接建一个 **65 字符以上用户名**的用户,用它 SSO 登录 → 应干净地判为无业务用户(前端登出),后端日志里有一条指名道姓的 WARN(「username is 70 chars, core_auth_user.username holds 64. Shorten the username in Keycloak.」),而**不是每个 API 都 500「Unhandled exception」**。恰好 64 字符仍能正常开通。此前 `OidcJitUserService` 的 JIT 开通把三个 token claim 直接塞进有宽度限制的列(`username` 64 / `display_name` 128 / `email` 255):我们自己的开通路径靠 DTO `@Size` 挡着,JIT 这条**完全绕过**,而 Keycloak 默认 user profile 允许用户名到 255、姓名是自由文本。已在真实库上验证 70 字符插入直接被拒(「对于可变字符类型来说,值太长了(64)」),该异常从 `resolveBusinessUserId` 冒出 `CoreRequestContextFilter` → 该用户**每一次**请求都 500,且报错里没有任何线索指向"用户名太长"。现在:用户名超宽 → 照"已删除用户"那条分支的姿态拒绝开通(WARN + 返回 null);`display_name` 是装饰性字段且由 `given_name + " " + family_name` 拼出来最容易超,故**截断**而非拒绝登录;`email` 不需要护栏(我们和 Keycloak 的列都是 255)。由 `OidcJitUserServiceTest` 钉住 3 个边界(65 拒 / 64 过 / 201 字显示名截到 128)。
- [ ] **审计行不再因为字段过长而被静默丢弃(bug 修复)**:构造一个超长 URL 的请求(`/api/system/users/` + 600 字符 id)去打任一带 `@OpLog` 的端点 → `core_oplog` **仍有那一行**,`request_uri` 截到 512。此前 `OpLogService.record` 把整个 insert 包在 catch-and-WARN 里(审计绝不能影响主流程),于是 Postgres 的「对于可变字符类型来说,值太长了(512)」**不报错、只是让这条审计消失**,只留一行 WARN。而 `OpLogAspect` 只对 `user_agent` 和 `error_msg` 做了 `safe(…, 500)`,`request.getRequestURI()` 原样透传;上面新加的两个 pre-auth `recordAudit` 更是一个都没截。已在真实库上验证 513 字符 `request_uri` 会被直接拒。修法放在**唯一的写入点**:`OpLogService.record` 按 schema 宽度逐列夹取(tenant_id/username/client_ip 64、module/target_type 32、action/target_id 64、request_uri/user_agent/error_msg 512、method 8;`request_body` 是 TEXT,不截,截了就毁审计载荷),这样任何生产者都不可能再因长度丢审计 —— 截断的审计行远好过没有。由 `OpLogServiceClampTest` 钉住(全超长 / 正常值原样 / 恰好等宽不掉字符 / null 仍是 null)。
- [ ] **邀请链接单次有效(重复使用 bug 修复)**:用 invite 链接设密成功后,**再次打开/提交同链接失效**(原子 `markUsed` 把 `used_at` NULL→now 并检查影响行数,0 行即拒绝;此前 SELECT+UpdateWrapper 不查行数,导致可重复设密)。链接也有时效(默认 7 天);重发会先作废旧链接,只最新有效。

---

## 3. 共通基盤（Cross-cutting）

- **时间模型(V58)**:全链路**绝对时刻**——DB 列 `timestamptz`、后端 `OffsetDateTime`(实体/DTO/`now()`,禁用 `LocalDateTime`,由 ArchitectureTest 守护)、wire 为 ISO-8601 **带偏移量**字符串(双向,前端发送走 `toBackendDate`)。业务时区只出现在边缘:前端显示(`@/lib/date` 的 `toJST*`,`VITE_DEFAULT_TIMEZONE`)与墙钟决策(后端 `AppTime.zone()`:cron 触发、日/月聚合 SQL 的 `AT TIME ZONE`、取号日期、邮件内时间文案)。面向**多国独立部署**:业务时区为部署级配置 `app.timezone`(env `CORE_TIMEZONE`),默认 `Asia/Tokyo`,非法 IANA id 启动即失败(`AppTimeConfigurer`);前后端两个时区配置应配成同一个值。显示结果与部署机器/浏览器时区无关。
- **字典(Dictionary)**:内置枚举字典 + 运行时可编辑的管理字典;前端 `useDict`,下拉/标签均走字典,不硬编码。`/platform/dicts` 管理。
- **操作审计(oplog)**:`@OpLog` 写 `core_oplog`;失败行带 `error_code`(BusinessException 的 4xx/7xx vs 未预期 500)。
- **领域事件出箱**:状态变更在同一事务 `EventPublisher.publish(...)` → `core_domain_event` → `OutboxDispatcher` → 可插拔 sink(当前为日志兜底)。详见 backend/AGENTS.md。
- **通知(铃铛)**:`core_notification` + SSE 心跳(15s)。注意与领域事件是两套东西。
- **编号(numbering)**:`core_numbering*`,按租户分配 user_no 等。**唯一的发号入口是 `NumberingService`**(原子 `UPDATE ... RETURNING`),任何路径都不得用 `MAX(业务列)+1` 自己算下一个号 —— 见 V59。
- **i18n**:ja_JP / zh_CN / zh_TW / en / ko_KR 五语;权限标签由后端 `I18nPermissionPatcher`(仅 dev)生成占位 `__TODO__`,需人工填 `frontend/src/lang/generated/permissions.*.json`。frontend 目录从 `user.dir` 向上逐级以 `package.json` 探测定位(兼容 IDEA 模块运行 / `spring-boot:run` 等不同工作目录),找不到时跳过且**不创建任何目录**;另一仓库布局可用 `app.permission.i18n.frontend-dir` 配绝对路径。
- **邮件品牌 logo**:所有邮件模板(`user-invite` / `user-direct-welcome` / `user-password-reset` / `user-break-glass-used`,各 5 语言)header 含品牌 logo;**外链 PNG** `${baseUrl}/access_matrix_logo.png`(由 `frontend/public/access_matrix_favicon.svg` 渲染成位图——邮件客户端不显示 SVG)。`MailService` 对每封邮件统一注入 `logoUrl`(`putIfAbsent`,调用方可覆盖)。

**测试点**
- [ ] 字典:新增/停用管理字典项;被引用的项不可删(报"in use")、枚举项不可删——这些是**业务拒绝**,不计入仪表盘"接口错误"。
- [ ] **定时任务 / 字典两个平台控制台按权限收敛按钮(bug 修复)**:给一个平台运维账号建一个**只读**角色(只勾 `platform:job:read` + `platform:dict:read`,不给 `:config` / `:run` / `:toggle` / `:create` / `:update` / `:delete`)→ ① 定时任务页:看不到「编辑 cron」「立即执行」按钮,状态列显示只读徽标(不是可点的开关),列表本身照常可看;② 字典页:看不到「新建字典类型」「新建字典项」以及两侧表格的编辑 / 删除按钮;③ 换成 `platform:*`(V55 的 PLATFORM_OPERATOR)则四个动作全部可见、可用。此前这两页是全项目**仅有的两个有写操作却零 `v-permission`** 的管理页(其余 Role / Dept / User / Tenant / Menu / Event / Task / PlatformUser 全都有):后端 `JobAdminController` / `DictAdminController` 是**逐个端点**用 `@RequiresPermission` 拦的(job 拆了 read/config/run/toggle 四个码、dict 拆了 read/create/update/delete 四个码),所以从来不是安全漏洞 —— 但控制台会把这些注定 403 的按钮摆在只读运维面前,等于对「你能做什么」撒谎。定时任务的开关是唯一特例:它同时也是**唯一显示启用状态的地方**,用指令直接删掉会连状态都看不见,所以改成 `usePermission()` 探测 `platform:job:toggle`,无权时退化成 `common_status` 字典渲染的只读徽标(遵循字典约定,不硬编码文案)。`buttonGating.test.js` 除了补上这两页的权限码清单,还新增了一组**源码漂移守卫**:直接读 6 个写操作管理页的 `.vue` 源码、断言每个写权限码确实出现在某个 `v-permission` 里 —— 原来的用例渲染的是合成按钮,一个页面**整个忘记加 `v-permission`** 也照样绿,这正是这两页当初漏掉的原因。已实测从 Job.vue 摘掉 `platform:job:run` 那一处即失败(`Job.vue is missing v-permission="'platform:job:run'"`)。共 28 条。
- [ ] **停用字典项后服务端也真的拒收(bug 修复)**:把 `task_priority` 的某个项停用(status=0)→ ① 新建/编辑 task 时下拉里没有它(本来就有);② **直接调 API** 传这个值(`POST /api/demo/task {"priority": <已停用值>}`,或用一个**停用前就打开的旧标签页**提交 —— 它的 dict 缓存里还有这个选项)→ 应返回 `error.dict.invalidValue`,不再写进库;③ 已经存着这个值的老行,**改标题等其它字段照样能存**(原值不变即放行),不会因为选项被退役而变成不可编辑;④ 从已停用值改成一个启用值正常。此前服务端唯一的校验器 `DictQueryService.isValidValue` **只看 `mark`、不看 `status`**,所以「停用」纯粹是前端行为。而这恰恰是唯一的退役手段 —— `DictAdminService.deleteItem` 自己的注释就写着:被代码分支引用或被业务数据引用的值**不能硬删**,「改为停用(status=0),这样历史行还能解析出标签」。读接口本来就按项返回 `enabled` 标志、前端 `useDict` 也确实分成了 `items`(全部,给标签用)/ `options`(仅启用,给 `<Select>` 用),只有服务端校验这一侧没跟上。现在拆成两个语义明确的方法:`isValidValue`(是否是本字典的合法值,历史行读取/原值沿用要它继续为真)与新增的 `isSelectableValue`(现在还允不允许被选),业务侧 `TaskService.validateDictValue` 走后者、并对「与库中原值相同」放行。由 `DictQueryServiceSelectableTest`(5 条)与 `TaskServiceDictGateTest`(7 条)钉住。
- [ ] **字典读缓存不会被随便一个登录用户撑爆(bug 修复)**:用任意**普通登录**账号连续请求不存在的字典码(`GET /api/dict/aaa`、`/api/dict/aab`… 几万次)→ 服务端内存占用应稳定,`DictQueryService` 的缓存条目数封顶(`MAX_CACHED_CODES=512`),真实字典码的命中率不受影响;改字典后 `evict` 依旧当场生效。此前该缓存是一个**无界 `ConcurrentHashMap`**,而它的 key 直接就是 `GET /dict/{code}` 的路径变量:这个端点按设计只认证、**不做权限校验**(每个登录用户都要靠它渲染表单下拉),读路径又按设计**容忍不存在的码**(返回空项列表而不是 404)。两个都是对的设计,合起来却成了「任何一个登录用户都能按请求数无上限地永久占内存」。现改为 Caffeine(`maximumSize` + 30 分钟写后过期兜底),与项目其它所有缓存(`CacheConfig` / `app.cache.specs`)的做法一致;显式 `evict` 保留,ops 改字典仍即时可见。由 `DictQueryServiceCacheBoundTest` 钉住 2 条(2 万个未知码后条目数 ≤ 512 / 真实码仍被缓存且可 evict),已实测换回 `ConcurrentHashMap` 即第 1 条失败(实测 20000)。
- [ ] **改完字典,已经开着的页签立刻跟着变(bug 修复)**:先打开任务页(下拉里有「高」),**不要关这个页签**,切到字典管理把 `task_priority` 的「高」停用(或新增一个「緊急」)→ 切回任务页,下拉里必须**立刻**没有「高」/多出「緊急」;同时列表里历史上已经是「高」的行**仍然显示「高」**这个标签(退役 ≠ 消失)。此前 `dictStore.invalidate(code)` 只把 `loaded` 置回 false 就返回了 —— 什么都不会重取。而 `useDict` 只在**组件 setup 里**调一次 `ensure`,业务页面又统统在 keep-alive(页签)后面,回到已开页签时 setup 根本不会重跑,于是那个页签的下拉整个会话都停在旧数据上。`Dict.vue` 调它的那行注释写的正是「让正在使用该字典的下拉刷新」——恰恰没发生。后果不只是显示旧选项:用户选了那个已退役的值提交,会被服务端 `TaskService.validateDictValue` → `isSelectableValue` 拒掉(见上一条),即 UI 刚刚还在推荐一个必定报错的选项。现在 `invalidate` 置位后**立即 `ensure`**;`ensure` 是原地改写共享的 reactive entry,所以一次重取就同时更新该 code 的**所有**存活消费者。重取失败时保留旧选项(不把下拉变空)。由 `src/stores/dict.test.js` 钉住 5 条(首次只取一次 / 已挂载消费者看到退役项消失但 `label()` 仍解析得出 / 已挂载消费者看到新增项 / 没人加载过的 code 不触发请求 / 重取失败保留旧值),已实测换回旧实现即 2 条失败。
- [ ] **改定时任务配置/启停后审计行有操作者(bug 修复)**:改某个 job 的 cron、或启用/停用它 → `core_oplog` 里对应的 `job.config` / `job.enable` / `job.disable` 行,`user_id`、`username`、`tenant_id` 必须是**发起这次修改的运维账号**,不再是空。此前这三个服务方法末尾都调 `reconcile()` → `DynamicJobScheduler.reconcileNow()`,而它是 `synchronized`、跑在**调用者线程**上;它内部的 `loadEnabled()` 为了跨租户读取,会把 `RequestContext` **覆盖**成 reconciler 身份、然后在 `finally` 里 **`clear()` 掉** —— 于是请求剩下的部分完全没有上下文了。而 `OpLogAspect` 恰恰是在 `finally` 里、**方法返回之后**才构造审计记录并读 `RequestContext.userId()/tenantId()/getUsername()`,所以这三条(唯一以 `reconcile()` 收尾的)端点把一次**特权调度器变更**记成了无主操作。按本项目自己在 pre-auth 审计那轮定下的标准:归属错误的审计行比没有更糟。现在 `loadEnabled()` 先快照调用者上下文、`finally` 里原样恢复(原本没有上下文的 `@Scheduled` 路径则仍然保持没有,不会残留 reconciler 身份污染线程池)。由 `JobAdminServiceContextTest` 钉住 3 条,且用的是**真实**调度器 bean(只 mock mapper)以免把待验证的逻辑 stub 掉;已实测把恢复逻辑换回 `clear()` 即复现原状(2 条断言失败)。
- [ ] **平台运维用户的 user_no 走统一发号器,不再和采番计数器打架(bug 修复)**:在平台运维用户管理里连建两个运维账号 → `user_no` 依次递增且不重复;随后让一个**直接在 Keycloak `system` realm 里建的**运维账号首次 SSO 登录(走 JIT 开通)→ 也能正常拿到号、正常访问,**不再每个请求 500**。此前 `PlatformUserAdminService.nextSystemUserNo()` 自己算 `MAX(CAST(SUBSTRING(user_no FROM 2) AS INTEGER))+1`,而项目其它所有开通路径(`UserAdminService.create` / `TenantAdminService.persistNewTenant` / `OidcJitUserService`)走的都是 `NumberingService`。手写那条**不推进任何计数器**,于是 `core_numbering_management` 里 `system` 的 `seq_id` 永远不知道这个控制台发过哪些号,两个发号器越走越偏,最终指向**同一个值**。已在真实库上验证到这一步:<br>`tenant_id=system | counter_now=1 | next_from_counter=U00000002 | max_in_use=U00000002 | next_already_taken=t`<br>`uk_core_auth_user_tenant_user_no` 是 `(tenant_id, user_no)` 上 `mark=1 AND user_no IS NOT NULL` 的部分唯一索引,所以撞号是**硬性插入失败**,受害者是下一个从计数器取号的人 —— 最可能是 `OidcJitUserService`:它对**取号**这一步包了 try/catch,但对随后的 `insert` **没有**,重复键会一路冒到 `CoreRequestContextFilter`,变成该用户**每一次**请求都 500。手写版本本身也是竞态的(两个并发创建读到同一个 MAX、格式化出同一个号),而 `NumberingService` 是单条 `UPDATE ... RETURNING` 原子自增。修复分两半:①代码改走 `numberingService.next("USER", "system")`;②**V59 迁移**把每个租户的 USER 计数器抬到它已发出的最大 `user_no`(`GREATEST` 所以幂等、已领先的不动;只认 `^U[0-9]+$` 形状;**软删行也计入**——`user_no` 是稳定的人可见编号,重发一个已注销用户用过的号比留个空洞更糟)。不修数据的话,改完代码后第一次取号仍会撞上已占用的号。V59 已在真实库上做过**事务内 dry-run + ROLLBACK**:`UPDATE 3`,之后 `system` 计数器 1→2、`demo` 1→15,四个租户的 `next_still_taken` 全部变 `f`。由 `PlatformUserAdminServiceTest` 新增 2 条钉住(创建走发号器且插入的就是发号器返回的值 / 不再出现任何 `SUBSTRING(user_no` 探测),已实测把那段手写逻辑放回去即 2 条失败。
- [ ] 任一带 `@OpLog` 的操作在 `core_oplog` 留痕;失败时 `error_code` 正确;操作日志页时间列按 **JST** 显示(与领域事件页同一时刻显示一致,不差 9 小时)。
- [ ] **带引号的密码不再被写进审计正文(安全缺口修复)**:用一个**含英文双引号**的密码建用户(`POST /user`,DIRECT 模式,例如 `Aa1!"secret`),或用同样的密码调 `POST /me/break-glass-password` → `core_oplog.request_body` 里必须是 `"password":"***"`,整行仍是**合法 JSON**,`secret` 那截不得出现在任何地方。此前掩码正则是 `"password"\s*:\s*"[^"]*"` —— JSON 字符串里的双引号是**转义**存放的(`"Aa1!\"secret"`),而 `[^"]*` 在那个反斜杠-引号处就停了,于是匹配止于**值的中间**:替换后得到 `{"username":"alice","password":"***"secret","email":"..."}` —— 引号之后的**整段明文进了审计表**,顺带把这行 JSON 弄成了语法错误(任何读 `core_oplog` 的消费者都解析不了)。这不是理论问题:`app.security.password-policy.require-symbol` 默认开启,双引号就是用户会选的普通符号;而 `POST /user`(管理员亲手输密码)和 `POST /me/break-glass-password` 两个端点都带 `@OpLog`、密码就在请求体里。`core_oplog` 是管理员可读、永久保留的,等于密码明文长期躺在那。现在值部分改成 `(?:\\.|[^"\\])*`(「一个转义序列,或既非引号也非反斜杠的字符」),正确吞掉转义对。已用同一对正则直接跑过对照:旧的输出 `"password":"***"secret"` 且 `json.loads` 失败,新的输出 `"password":"***"` 且可解析。由 `OpLogAspectMaskingTest` 钉住 6 条(普通密码 / 含引号密码 / 以反斜杠结尾的密码 / 掩码后仍是合法 JSON / 6 个密码字段名大小写不敏感全覆盖 / 无密码的正文原样保留),已实测换回旧正则即 3 条失败。
- [ ] **Actuator 暴露面收紧**:未认证访问 `/api/actuator/metrics` / `/prometheus` / `/caches` / `/info` 返回 **401**;`/api/actuator/health/**`(含 liveness/readiness 探针)始终公开;持有效 token 访问 metrics 返回 200。仅 `/actuator/health/**` 在 `PERMIT_PATHS`。
- [ ] **`X-Tenant-Id` 头不再能注入 SQL(安全漏洞修复)**:对任一 pre-auth 端点(如 `POST /api/auth/login`)发 `X-Tenant-Id: x' OR '1'='1` → 请求被当作**没带该头**处理(落回默认租户 `demo`),日志出现一条 WARN(只记长度、不记值);发 `X-Tenant-Id: '` 不再 500。合法值(`demo` / `sozonext` / `acme-inc` / `system`,以及任意 RFC1035 标签)行为不变。此前该头只做了 `.trim()`、**没有任何校验**,而 `MybatisPlusConfig` 把它交给 `TenantLineInnerInterceptor` 的方式是 `new StringValue(tid)` —— MyBatis-Plus 把它拼进 SQL **文本**,不是绑定参数;jsqlparser 的 `StringValue` **不转义**(构造参数名就叫 `escapedValue`,`toString()` 只是给值加一对引号)。已用 classpath 上的 jsqlparser 直接验证:`new StringValue("x' OR '1'='1").toString()` 得到 `'x' OR '1'='1'`;再用**真实的 `TenantLineInnerInterceptor`** 复现出改写后的完整语句:`SELECT * FROM core_auth_user WHERE username = 'alice' AND tenant_id = 'x' OR '1'='1'` —— `AND` 优先于 `OR`,整个 WHERE 变成 `(username='alice' AND tenant_id='x') OR ('1'='1')`,**匹配全表**,不只是越租户。落到 `/auth/login` 上就是:`findByIdentifier` 的租户隔离退化成恒真,一个名义上指向不存在租户的登录请求可以匹配到**任意租户**的同名用户,接下来只需要那个人的密码。注意仅 pre-auth 可达(已认证请求的租户取自 JWT `tid` claim,不看这个头)。修法是在入口把该头规范化成系统各处本来就假定的 RFC1035 标签形状(与 `TenantDto.CreateRequest.tenantCode` 的正则、前端 `utils/tenant.js` 的校验、以及 Keycloak realm 名的约束一致),不合法即视为未提供;顺带把值限制在 63 字符内,也就同时落在 64 宽的 `tenant_id` 列里。由 `TenantHeaderInjectionTest` 钉住 4 条(含直接断言那条被改写出来的完整 SQL)。
- [ ] **客户端 IP 不默认信任 XFF**:默认 `CORE_FORWARD_HEADERS_STRATEGY=none` + `CORE_TRUST_FORWARDED_HEADERS=false` 下,轮换 `X-Forwarded-For` 不能绕过 `/auth/*` 按 IP 限流(都落到真实 socket IP),且 `core_auth_login_log.client_ip` 记录真实对端而非伪造头值。反代部署时两者**成对**设 `framework`/`true`,取 XFF 最右一跳。
- [ ] **限流器自身不再是内存耗尽面(bug 修复)**:对 `/auth/*` 用**大量不同源 IP**打请求(例如脚本轮换 10 万个地址)→ 进程内存占用应当趋于平稳,不随见过的 IP 数线性增长;同时限流本身**一点不放松**:同一个 IP 连打仍在第 `requests-per-minute+1` 次拿到 **429** + `Retry-After`,另一个 IP 不受牵连。此前 `AuthRateLimitFilter` 的桶存在一个**从不删元素**的 `ConcurrentHashMap<String, Bucket>` 里 —— 每个访问过 `/auth/` 的不同 IP 都会留下一个 bucket 直到 JVM 退出。对外网部署来说,光是正常流量就会无界增长;而只要开了 `CORE_TRUST_FORWARDED_HEADERS=true`,攻击者轮换 `X-Forwarded-For` 就能以极低成本把**限流器本身**变成内存耗尽向量 —— 偏偏它正是那个用来兜住暴力破解的组件。改成 Caffeine(`maximumSize=100000` + `expireAfterAccess=refill-period`)。淘汰对安全性质是无害的:bucket 里只记**已消耗**的令牌,丢掉它绝不可能让某个 IP 变得**更**受限;而条目至少保留一个完整 refill 周期,过了这个周期 bucket 本就会回满,所以闲置淘汰没有丢弃任何仍在限流的状态;还在持续打请求的攻击者每次访问都会刷新 TTL,淘汰不到他头上。由 `AuthRateLimitFilterTest` 钉住 4 条(预算内放行/超出 429、一个 IP 打爆不影响另一个、非 `/auth/` 路径完全不进桶、10 万上限下轮换 15 万个 IP 后仍远低于 15 万)。
- [ ] **连续用脚手架建两个模块不再撞 Flyway 版本号(bug 修复)**:用 `--new-module=` 连着建两个业务模块 → 两份迁移的版本号应为 `V1000` 与 `V1001`,启动正常;不再两个都是 `V1000`(那样 Flyway 直接报「Found more than one migration with version 1000」拒绝启动)。此前**写入**和**读取**的目录不是同一个:新模块模式把迁移写进模块自己的 `src/main/resources/db/migration`,却从 **core-bootstrap** 的目录去取下一个版本号 —— 后者永远不会因为前面建过模块而变化,于是每次都算出同一个数。而 `spring.flyway.locations` 是 `classpath:db/migration`,Flyway 会跨所有 jar/classes 目录解析,这些目录共享**同一个版本空间**(模板注释里"business 从 V1000 起、框架占 V1-V999"的约定正是这个意思)。旧模式(往 business-demo 加资源)在有了模块级迁移之后同样会撞。现在版本选取扫描**全部**迁移目录(core-bootstrap + 每个 `backend/*/src/main/resources/db/migration`)。由 `BusinessModuleScaffoldVersionTest` 钉住 6 条(只有框架迁移时从 1000 起 / 模块迁移被计入所以下一次是 1001 / 跨目录取最大 / 目录发现只收真有迁移的模块 / 目录缺失不崩 / 非迁移文件被忽略)。
- [ ] **启动兜底 DDL 与迁移基线对齐(bug 修复)**:`AuthSchemaBootstrap` 是 `flyway_schema_history` 脏掉时的 `IF NOT EXISTS` 兜底网,健康库上每条语句都是空操作 —— 恰恰因此它的漂移**在唯一要紧的那一刻之前完全不可见**,而且一旦真的触发就再也没人纠正:能塑形这些表的迁移早已被记为已执行。核对真实库后发现两处漂移:①**21 个**审计/时间列全是 `TIMESTAMP ... DEFAULT CURRENT_TIMESTAMP`(无时区、零个 `TIMESTAMPTZ`),而真实库里这些列**全部**已是 `timestamp with time zone` / `now()` —— 正是 V58 专门要消灭的状态,也正是上一条脚手架模板犯过的同一种错;真触发时 `BaseEntity` 的 `OffsetDateTime` 会被按写入方 JVM 时区强转落库,回读丢偏移量、JST 投影静默偏移。②`core_numbering_management` 仍是 **V31 之前的全局形状**(没有 `tenant_id`、主键单列 `code_kbn`),而真实库是 `tenant_id NOT NULL` + 主键 `(tenant_id, code_kbn)`;真触发时 `NumberingService` 的每一条 `WHERE tenant_id = ? AND code_kbn = ?` 都会因缺列报错,`TenantSchemaGuard` 也会拒绝启动(它做得对,但那时错误的表结构已经建好了)。文件里本来就有「mirror the post-V18 / post-V19 / post-V20 / post-V33 layout」这类注释 —— 跟住迁移本就是这个文件的契约,只是 V31 与 V58 两次没跟上。修复:21 列全改 `TIMESTAMPTZ NOT NULL DEFAULT now()`;numbering 表补 `tenant_id` 并改成复合主键,连**约束名**都对齐 V31 的 `core_numbering_management_pkey`,好让从零建库与 Flyway 建库无法区分。由 `AuthSchemaBootstrapDdlTest` 钉住 5 条(无任何无时区 `TIMESTAMP` / 无 `CURRENT_TIMESTAMP` / 每个时间列都是 `TIMESTAMPTZ`+`now()` / numbering 表按租户且主键与约束名匹配 V31 / `core_numbering_key` 保持三列主键),断言只读源码不依赖数据库——目的就是让「改了迁移却忘了改兜底网」当场编译期失败。已实测把两处漂移各回退一次即 4 条失败。
- [ ] **脚手架生成的迁移不再撤销 V58(bug 修复)**:用 `BusinessModuleScaffold` 生成一个新业务模块,打开它产出的 `V####__create_business_xxx.sql` → 时间列必须是 `TIMESTAMPTZ NOT NULL DEFAULT now()`,不能是 `TIMESTAMP ... DEFAULT CURRENT_TIMESTAMP`。此前模板生成的正是后者 —— 而 V58 整支迁移就是专门把所有时间列从**无时区** `timestamp`(该迁移自己的话:「meaning depended on the writing JVM's default zone」)转成 `timestamptz`,并**特意**把默认值重建为 `now()` 而不是 `CURRENT_TIMESTAMP`(避免 PostgreSQL 把旧默认值包成随会话变化的双重 cast)。脚手架是 AGENTS.md「业务代码配方」里**官方指定的建表方式**,所以这一处滞后会被**每个新模块**原样复制:生成出来的列是无时区的,而实体继承 `BaseEntity`、审计字段是 `OffsetDateTime`(`LocalDateTime` 被 ArchUnit 禁止),正好回到 V58 存在的理由。Flyway 不会察觉 —— 文件写什么它就建什么。由 `BusinessModuleScaffoldMigrationTest` 钉住 5 条(timestamptz+now / 不含任何无时区时间列与 `CURRENT_TIMESTAMP` / `tenant_id`+`mark`+`CHAR(26)` 主键三项约定仍在 / 唯一索引以 tenant 领先且 `WHERE mark = 1` / 表名与版本号正确接线)。
- [ ] 时间模型:API 返回的时间戳都是**带偏移量**的 ISO 串(通常为 UTC 形如 `2026-06-10T08:38:33Z`;偏移量数值不承载语义,仅标注本次投影用的钟);把浏览器/操作系统改成任意时区,所有页面时间显示不变(仍为 JST);仪表盘 24h/7d/30d 统计窗口及按日/月聚合按 JST 切日。
- [ ] **超长 `X-Trace-Id` 不再让业务写入整体失败(bug 修复)**:带 `X-Trace-Id: <200 字符>` 调一个会发领域事件的写接口(如 `POST /api/demo/task`)→ **正常创建成功**,`core_domain_event.trace_id` 为该值的前 64 字符,响应回显的 `X-Trace-Id` 也是同一个截断值(不能回显一个库里查不到的值,否则关联不上)。此前 `CoreRequestContextFilter` 把这个请求头**原样**放进 `RequestContext`,而 `OutboxEventPublisher` 是**故意**把出箱行插进调用方事务里的(这正是 transactional-outbox 的保证),`core_domain_event.trace_id` 只有 64 宽:已在真实库上验证 65 字符被拒、64 通过 → 于是这不像审计那样降级成"丢一行",而是**整个业务写入回滚**、返回不透明的 500。这个头正是网关 / APM agent 会自动打的(W3C traceparent 55 字符尚可,再拼一层就爆),等于一个反代配置就能让所有发事件的写操作全挂。修在**入口单点**(而不是发布器里),这样 `RequestContext`、出箱行、回显头三处看到的是同一个值。缺失/空白仍生成 32 字符 UUID hex。由 `CoreRequestContextFilterTraceIdTest` 钉住。
- [ ] 访问不存在的 API 路径(如 `GET /api/platform/jobs`)→ HTTP **404**(`JsonResult` code=404),服务端无「Unhandled exception」ERROR 日志,不计入仪表盘「接口错误(24h)」。
- [ ] 语言切换:5 语言下新功能文案都不出现 key 原文 / `__TODO__`。
- [ ] **邮件模板做 HTML 转义(安全缺口修复)**:用一个构造过的 `User-Agent`(如 `</td></tr></table><style>body{display:none}</style><a href="http://evil">x</a>`)做一次 break-glass 登录 → 账号本人收到的告警邮件里这段应显示为**纯文本**(`&lt;style&gt;…`),邮件结构完好、「立即轮换」按钮仍在,不再被这段标记改写或整封清空。此前模板用 `.ftl` 后缀、`spring.freemarker.settings` 里没有 `output_format`,也没有任何模板声明 `<#ftl output_format="HTML">` —— 即 `${...}` 是**原样输出**。而 `user-break-glass-used` 恰恰把这次登录的 `User-Agent` 与 client IP 渲染进邮件,这封邮件的全部意义就是告诉账号本人「不是你就赶紧轮换」并给一个红色轮换按钮:不转义时,已经拿到应急密码的人可以顺手把告警邮件改写、清空,或把轮换按钮换成钓鱼链接,正好废掉这个控制点。修法是在 `application.yml` 打开 `spring.freemarker.settings.output_format: HTML`(全局;`MailService` 用的就是 Spring 装配的 `FreeMarkerConfigurer`)—— 模板里所有插值都是纯文本或 URL,而 URL 在 `href`/`src` 属性里被转义成 `&amp;` 恰好也是 HTML 属性里正确的写法,故全局打开安全;将来要输出标记的地方用 `${x?no_esc}` 显式声明。由 `MailTemplateEscapingTest` 钉住 4 条:恶意 UA 被转义且邮件结构完好、带 query 的 URL 转义后仍可用、**4×5=20 个模板在 HTML 模式下全部正常渲染**(无一需要 `?no_esc`)、以及一条防漂移断言(直接检查 `application.yml` 里那行还在 —— 渲染测试自己配置输出格式,配置丢了它们也照样绿)。已实测把该设置从测试里摘掉即复现原状(断言失败)。
- [ ] 通知:触发后铃铛出现未读、可标记已读。
- [ ] **删除任务会一并消掉它的待办通知(bug 修复)**:给 B 分配一条 task(B 的铃铛出现「対応が必要なタスクが割り当てられました」未读)→ 由 A **删除**这条 task → B 的未读数应当立刻减掉这一条(SSE 推新计数),铃铛里也不再留着这条。此前只有把状态改成**完了 / 取消**才会发 `NotificationResolvedEvent` → `markReadByBiz` 把该 `bizType+bizId` 的未读 action 通知批量置读;**删除这条路径没发**,于是 B 的待办永远挂在那儿:既没有任何后续动作能解掉它(任务行已经 `mark=0`,再也不会有状态变更),点开还会走 `openById` → `get(id)` → `loadVisibleOr404` 抛 NOT_FOUND,弹一个错误 toast。`update()` 那处的注释写的是「処理完了 = 完了/取消,此时把该任务的 action 通知置读」—— 删除显然比完了更终局,漏的就是这一处对称性。由 `TaskServiceNotificationResolveTest` 钉住 3 条(删除会发解决事件且 bizType/bizId/tenant 正确 / 改成完了仍会发 / 普通改标题不会发),已实测把 `delete()` 里那次 publish 摘掉即失败。
- [ ] **SSE ticket 严格一次性(bug 修复)**:`POST /notification/sse-ticket` 拿到 ticket 后,`GET /notification/stream?ticket=X` **只能成功一次**——重放同一个 ticket(哪怕在 60s TTL 内、哪怕并发同时发两个)第二个连接立即关闭、拿不到任何推送。消费改成原子 `GETDEL`(与 `RefreshTokenStore.rotate` 同一把锁),此前是 GET 后再 DELETE,两个并发请求会双双读到非空值、双双拿到已鉴权的流。这条路径特别值得较真:`/notification/stream` 在 `SecurityConfig.PERMIT_PATHS` 里(EventSource 发不了 Authorization 头),ticket 是唯一凭据,而它走在 **query string** 上——会落进反代/访问日志与浏览器历史,重放是现实攻击面而非理论。ticket 无效/过期/已用 → 返回已 complete 的空流(不抛异常,否则异常处理器会往 `text/event-stream` 上渲染 JSON 变成 406),客户端自行重新申请。由 `NotificationControllerTest` 钉住。
- [ ] **刷新标签页后红点仍然实时更新(bug 修复)**:在通知页/任一挂着铃铛的页面上**反复刷新**(或快速来回切路由),然后从别处触发一条新通知 → 红点应当**立刻**变(SSE 推送),而不是要等最多 20s 的轮询兜底、更不是要等 30 分钟 SSE 超时重连。此前 `SseEmitterRegistry` 的注册是 `conns.computeIfAbsent(key,…)` 拿到 list、**再**在 map 的 bin 锁之外 `list.add(emitter)`,而移除是 `conns.get(key)` → 改 list → 空了就 `conns.remove(key)`。两者可以这样交错:注册方拿到 list → 移除方把它清空并**把 key 从 map 里摘掉** → 注册方才把新 emitter 加进这个**已经不可达**的 list。于是这条连接活着、浏览器也以为连上了,但 `sendUnread` 按 key 查不到它,推送全部落空 —— 而"刷新标签页"正好就是这个交错(新连接建立时旧连接正在拆除)。现在注册的 add 放进 `conns.compute(...)` 内部、移除走 `computeIfPresent`(返回 `null` 即原子摘 key),两者互斥。顺带把 `sendUnread` / 心跳里发送失败后的 `list.remove(em)` 也统一改走 `remove(...)`,否则最后一个 emitter 掉线后会在 map 里永远留下一个空 list。由 `SseEmitterRegistryTest` 钉住 5 条,其中并发用例(两线程各 2 万轮:一边不断建/拆、一边建完即推并检查有没有收到)已实测把这两处改回旧写法即稳定复现(断言失败)。
- [ ] **离开页面后不再留下关不掉的 SSE 连接(bug 修复)**:在铃铛所在页面**刚打开就立刻切走**(或直接登出),重复十几次 → 后端 `core_notification` 的 SSE 连接数不应堆积,浏览器 Network 面板里也不该留下一串永远 pending 的 `notification/stream`。此前 `useNotificationStream.connect()` 只在**进入函数时**判了一次 `stopped`,随后要 `await getSseTicketApi()`;组件如果正好在这个 await 期间卸载,`onUnmounted` 看到的 `es` 还是 `null`(连接还没建),关无可关,而 await 之后的续行仍会照建一个 `EventSource` —— 没有任何变量再持有它,于是它永远不会被 `close()`,对应的后端 `SseEmitter` 也要挂到 30 分钟超时才释放,每次这样的导航泄一条。现在在 await 之后重新判一次 `stopped`。由 `useNotificationStream.test.js` 钉住 2 条(await 期间卸载 → 一个 EventSource 都不建;正常挂载 → 带 ticket 建连且卸载时确实被 close),已实测去掉这次判断即复现(断言失败)。
- [ ] 邮件 header 显示品牌 logo:在 Gmail/Outlook 等也能显示(因用 **PNG** 而非 SVG);logo 加载不出时下方文字「Access Matrix」兜底。`logoUrl` 指向 `${baseUrl}/access_matrix_logo.png`(前端静态资源,需前端已部署/可访问)。

---

## 4. フロントエンド基盤（Frontend foundation）

- **多 Tab + keep-alive**:目录占位 `EmptyLayout` 单实例复用(修复了"多 tab 后新 tab 发重复请求")。
- **Tab 栏(Browser 风)**:`AppTabBar`。非激活 tab 之间用 `::before` 竖分割线分隔(相邻激活/hover 时隐藏);tab `flex-1 min-w-[56px] max-w-[120px]` + `truncate` 均匀缩小;缩到极限不向右延伸,多出的收进右侧 `+N`(`ResizeObserver` 测宽算 `visibleCount`,始终保证 active 可见),hover `+N` 弹出隐藏 tab 列表;标题被截断时 hover 在 tab 下方弹完整名 tooltip(`pointer-events-none`,Teleport 到 body 避免被裁,仅 `scrollWidth > clientWidth` 时弹)。拖拽排序 / 右键菜单 / 末端批量操作保留。
- **详细 tab 业务标识**:`tabsStore.tabExtras`(**不持久化**的内存 map,`fullPath → {prefixKey, badge}`)+ `setTabExtra`。业务详细页在自身 `loadDetail` 内调用(切忌用依赖全局 route 的 watch,keep-alive 缓存下会污染别的 tab);`tabLabel` 读它把 `予約番号/施設名` 等业务标识拼到「XX-詳細」后,`prefixKey` 存 i18n key 以跟随语言。基盘暂无业务详细页,此为预置基础设施。
- **确认弹窗**:全项目用 `useConfirm()` 的 `confirm()`(Promise)+ 常驻 `<ConfirmDialog>`(无 `window.confirm`);`ConfirmDialog` z-index = `z-[100]`,高于 `Dialog`(`z-[90]`),保证从 Dialog 内触发删除时确认框可见可点(否则被遮 → Promise 永不 resolve → "点击无反应")。
- **侧边栏**:收藏 / 置顶 / 顶层叶子菜单提升 / 折叠 flyout。
- **主题 / 调色板**:CSS 变量;`.dark` + `data-palette`。单一暖色主题(暖米白 `#f4efe9` 背景 + 纯白卡片 + 页面背景两团极淡品牌径向光晕,深色模式去掉光晕),仅**品牌强调色**可切:陶土铁锈(默认)/ 砖红 / 赭黄 / 暗松绿 / 灰紫;surfaces 五色共用,只换 `--primary/--accent/--ring/--brand-orange`。**深色用 `.dark` class 切换**(`useTheme.js`),`main.css` 必须配 `@custom-variant dark (&:where(.dark, .dark *))`,否则 Tailwind v4 的 `dark:` 默认跟随系统 `prefers-color-scheme`,"系统亮色 + 手动深色"时全项目 `dark:` 静默失效。主题在 app **启动时即应用**(`main.js` 引入 `useTheme`,模块副作用给 `<html>` 加 `.dark`/`data-palette`),使**未登录的 `/login` 等首屏**也跟随;否则 `useTheme` 只被登录后的 `AppHeader` 引入,登录页会停在亮色、且 SSO 跳转会误传 `ui_mode=light`。

**测试点**
- [ ] 打开多个 tab,再开新 tab:每个接口**只发一次**(无重复请求)。
- [ ] **改「每页条数」后不会掉进空白页(bug 修复)**:找一个数据量能翻页的列表(用户 / 操作日志 / 任务 / 角色 / 领域事件 / 定时任务 / 运维用户,任意一个走 `DataTable` 的都行),翻到**最后一页**(例如 25 条、10 条/页 → 第 3 页),再把「每页条数」调大到 20 → 应当**自动回到第 1 页并显示数据**,而不是空表格 + 页脚写着「3 / 2」。此前 `DataTable.changePageSize` 只 emit 了 `update:pageSize`;而所有调用方清一色是 `v-model:page` + `v-model:page-size` + `@update:page/@update:page-size="fetchData"` 的接法,于是带着旧 `page` 去请求 `page=3&size=20`,后端翻过末页返回空集。各页面里那句 `page.value = 1` 只挂在搜索/重置按钮上,接不住这条路径。修在共享组件里一处,8 张表一起生效;回到第 1 页也与本项目唯一手写分页(`Tenant.vue` 的 `watch([statusFilter, pageSize], () => { page.value = 1 })`)一致。先 emit `update:page` 再 emit `update:pageSize`,保证后一次 fetch 拿到的是 (page=1, 新 size) 这个正确组合;已经在第 1 页时不多发一次事件。由 `tests/components/dataTablePageSize.test.js` 钉住 3 条,已实测撤掉修复即 2 条失败。
- [ ] 关闭 tab 不残留 404;双击 tab 刷新该页。
- [ ] Tab 栏:非激活 tab 间有竖分割线(激活/hover 及其相邻处隐藏);窗口变窄时 tab 均匀缩小并 `…` 截断,不向右溢出;再窄则多出的收进 `+N`,active 始终可见;hover `+N` 弹隐藏列表可点选/关闭;hover 被截断的 tab 在其下方弹完整名 tooltip。
- [ ] 删除确认:从抽屉/Dialog **内部**触发删除时,确认框浮在最上层可点(不被 Dialog 遮);取消/确认都能正常 resolve。
- [ ] 收藏/置顶菜单显示在顶部;折叠侧栏 hover 出 flyout。
- [ ] **侧边栏「管理者設定」分组与固定底部区域真的能出现(bug 修复)**:在菜单管理里把某个一级菜单的 `sortOrder` 改成 `9500` → 它应从常规区域移入侧边栏的**管理者設定**分组;改成 `10500` → 移入**固定底部**区域;改回 `<= 9000` 回到常规区域。此前这四个分组读的是 `m.sort` —— 后端 `MenuNode` 下发的字段叫 **`sortOrder`**(`/menu/me`、`MenuPicker`、菜单管理页全都是这个名字),`undefined ?? 0 === 0` 让所有菜单一律落进常规区域,那两个区域**永远为空**:模板和 i18n(`layout.sidebar.adminGroup`)都在,却一次也渲染不出来,管理员改了 sortOrder 也毫无反应、毫无报错。当前库里所有菜单 sortOrder 都在 0~90,所以这个 bug 此前没有可见症状 —— 属于配置一改就现形的潜伏型。阈值判定已抽成 `@/lib/menu-groups` 纯函数,由 `src/lib/menu-groups.test.js` 钉住(含"四个区域互斥且覆盖全部可见菜单")。
- [ ] 切主题/调色板,图表与界面颜色随之更新。
- [ ] **深色模式**:系统设为亮色、应用内手动切深色,所有 `dark:` 工具类(登录页 / 租户行 / 徽标 / Header / Break-glass 弹窗等)正常生效,无局部变白。
- [ ] **登录页跟随主题**:选深色后退出到 `/login`(或 SSO 跳转过渡页),登录页本身为深色(`<html>` 启动即带 `.dark`);此时点 SSO 登录,KC 也收到 `ui_mode=dark`(印证 `currentUiMode()` 读得到 `.dark`)。
