# Root-level .gitignore — cross-stack only.
# Each stack maintains its own under backend/.gitignore and frontend/.gitignore
# (Maven targets / Node modules etc.) so the root stays small and language-neutral.

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr
.fleet/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor swap / backup
*.swp
*.swo
*~
.*.kate-swp

# Local dev screenshots taken via Chrome DevTools MCP while iterating on
# Keycloak theme / frontend layout. Useful while debugging but not worth
# committing — they bloat the repo and go stale immediately.
.dev-screenshots/

# Claude Code per-user override (skill prompts under .claude/commands/ stay committed)
.claude/settings.local.json
